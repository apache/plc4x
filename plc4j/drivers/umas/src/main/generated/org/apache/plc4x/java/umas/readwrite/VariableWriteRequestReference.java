/*
 * Licensed to the Apache Software Foundation (ASF) under one
 * or more contributor license agreements.  See the NOTICE file
 * distributed with this work for additional information
 * regarding copyright ownership.  The ASF licenses this file
 * to you under the Apache License, Version 2.0 (the
 * "License"); you may not use this file except in compliance
 * with the License.  You may obtain a copy of the License at
 *
 *   https://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing,
 * software distributed under the License is distributed on an
 * "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
 * KIND, either express or implied.  See the License for the
 * specific language governing permissions and limitations
 * under the License.
 */
package org.apache.plc4x.java.umas.readwrite;

import static org.apache.plc4x.java.spi.codegen.fields.FieldReaderFactory.*;
import static org.apache.plc4x.java.spi.codegen.fields.FieldWriterFactory.*;
import static org.apache.plc4x.java.spi.codegen.io.DataReaderFactory.*;
import static org.apache.plc4x.java.spi.codegen.io.DataWriterFactory.*;
import static org.apache.plc4x.java.spi.generation.StaticHelper.*;

import java.time.*;
import java.util.*;
import org.apache.plc4x.java.api.exceptions.*;
import org.apache.plc4x.java.api.value.*;
import org.apache.plc4x.java.spi.codegen.*;
import org.apache.plc4x.java.spi.codegen.fields.*;
import org.apache.plc4x.java.spi.codegen.io.*;
import org.apache.plc4x.java.spi.generation.*;

// Code generated by code-generation. DO NOT EDIT.

/** Variable write request reference — identifies a variable to write with data */
public class VariableWriteRequestReference implements Message {

  // Properties.
  protected final byte isArray;
  protected final byte dataSizeIndex;
  protected final int block;
  protected final int baseOffset;
  protected final int offset;
  protected final Integer arrayLength;
  protected final byte[] recordData;

  public VariableWriteRequestReference(
      byte isArray,
      byte dataSizeIndex,
      int block,
      int baseOffset,
      int offset,
      Integer arrayLength,
      byte[] recordData) {
    super();
    this.isArray = isArray;
    this.dataSizeIndex = dataSizeIndex;
    this.block = block;
    this.baseOffset = baseOffset;
    this.offset = offset;
    this.arrayLength = arrayLength;
    this.recordData = recordData;
  }

  public byte getIsArray() {
    return isArray;
  }

  public byte getDataSizeIndex() {
    return dataSizeIndex;
  }

  public int getBlock() {
    return block;
  }

  public int getBaseOffset() {
    return baseOffset;
  }

  public int getOffset() {
    return offset;
  }

  public Integer getArrayLength() {
    return arrayLength;
  }

  public byte[] getRecordData() {
    return recordData;
  }

  public void serialize(WriteBuffer writeBuffer) throws SerializationException {
    PositionAware positionAware = writeBuffer;
    boolean _lastItem = ThreadLocalHelper.lastItemThreadLocal.get();
    writeBuffer.pushContext("VariableWriteRequestReference");

    // Simple Field (isArray)
    writeSimpleField("isArray", isArray, writeUnsignedByte(writeBuffer, 4));

    // Simple Field (dataSizeIndex)
    writeSimpleField("dataSizeIndex", dataSizeIndex, writeUnsignedByte(writeBuffer, 4));

    // Simple Field (block)
    writeSimpleField("block", block, writeUnsignedInt(writeBuffer, 16));

    // Simple Field (baseOffset)
    writeSimpleField("baseOffset", baseOffset, writeUnsignedInt(writeBuffer, 16));

    // Simple Field (offset)
    writeSimpleField("offset", offset, writeUnsignedInt(writeBuffer, 16));

    // Optional Field (arrayLength) (Can be skipped, if the value is null)
    writeOptionalField("arrayLength", arrayLength, writeUnsignedInt(writeBuffer, 16));

    // Array Field (recordData)
    writeByteArrayField("recordData", recordData, writeByteArray(writeBuffer, 8));

    writeBuffer.popContext("VariableWriteRequestReference");
  }

  @Override
  public int getLengthInBytes() {
    return (int) Math.ceil((float) getLengthInBits() / 8.0);
  }

  @Override
  public int getLengthInBits() {
    int lengthInBits = 0;
    VariableWriteRequestReference _value = this;
    boolean _lastItem = ThreadLocalHelper.lastItemThreadLocal.get();

    // Simple field (isArray)
    lengthInBits += 4;

    // Simple field (dataSizeIndex)
    lengthInBits += 4;

    // Simple field (block)
    lengthInBits += 16;

    // Simple field (baseOffset)
    lengthInBits += 16;

    // Simple field (offset)
    lengthInBits += 16;

    // Optional Field (arrayLength)
    if (arrayLength != null) {
      lengthInBits += 16;
    }

    // Array field
    if (recordData != null) {
      lengthInBits += 8 * recordData.length;
    }

    return lengthInBits;
  }

  public static VariableWriteRequestReference staticParse(ReadBuffer readBuffer)
      throws ParseException {
    readBuffer.pullContext("VariableWriteRequestReference");
    PositionAware positionAware = readBuffer;
    boolean _lastItem = ThreadLocalHelper.lastItemThreadLocal.get();

    byte isArray = readSimpleField("isArray", readUnsignedByte(readBuffer, 4));

    byte dataSizeIndex = readSimpleField("dataSizeIndex", readUnsignedByte(readBuffer, 4));

    int block = readSimpleField("block", readUnsignedInt(readBuffer, 16));

    int baseOffset = readSimpleField("baseOffset", readUnsignedInt(readBuffer, 16));

    int offset = readSimpleField("offset", readUnsignedInt(readBuffer, 16));

    Integer arrayLength =
        readOptionalField("arrayLength", readUnsignedInt(readBuffer, 16), (isArray) != (0));

    byte[] recordData =
        readBuffer.readByteArray(
            "recordData",
            Math.toIntExact(
                (((isArray) == (1)) ? (dataSizeIndex) * (arrayLength) : dataSizeIndex)));

    readBuffer.closeContext("VariableWriteRequestReference");
    // Create the instance
    VariableWriteRequestReference _variableWriteRequestReference;
    _variableWriteRequestReference =
        new VariableWriteRequestReference(
            isArray, dataSizeIndex, block, baseOffset, offset, arrayLength, recordData);
    return _variableWriteRequestReference;
  }

  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (!(o instanceof VariableWriteRequestReference)) {
      return false;
    }
    VariableWriteRequestReference that = (VariableWriteRequestReference) o;
    return (getIsArray() == that.getIsArray())
        && (getDataSizeIndex() == that.getDataSizeIndex())
        && (getBlock() == that.getBlock())
        && (getBaseOffset() == that.getBaseOffset())
        && (getOffset() == that.getOffset())
        && (getArrayLength() == that.getArrayLength())
        && (getRecordData() == that.getRecordData())
        && true;
  }

  @Override
  public int hashCode() {
    return Objects.hash(
        getIsArray(),
        getDataSizeIndex(),
        getBlock(),
        getBaseOffset(),
        getOffset(),
        getArrayLength(),
        getRecordData());
  }

  @Override
  public String toString() {
    WriteBufferBoxBased writeBufferBoxBased = new WriteBufferBoxBased(true, true);
    try {
      writeBufferBoxBased.writeSerializable(this);
    } catch (SerializationException e) {
      throw new RuntimeException(e);
    }
    return "\n" + writeBufferBoxBased.getBox().toString() + "\n";
  }
}
