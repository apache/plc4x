/*
 * Licensed to the Apache Software Foundation (ASF) under one
 * or more contributor license agreements.  See the NOTICE file
 * distributed with this work for additional information
 * regarding copyright ownership.  The ASF licenses this file
 * to you under the Apache License, Version 2.0 (the
 * "License"); you may not use this file except in compliance
 * with the License.  You may obtain a copy of the License at
 *
 *   https://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing,
 * software distributed under the License is distributed on an
 * "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
 * KIND, either express or implied.  See the License for the
 * specific language governing permissions and limitations
 * under the License.
 */
package org.apache.plc4x.java.umas.readwrite;

import static org.apache.plc4x.java.spi.codegen.fields.FieldReaderFactory.*;
import static org.apache.plc4x.java.spi.codegen.fields.FieldWriterFactory.*;
import static org.apache.plc4x.java.spi.codegen.io.DataReaderFactory.*;
import static org.apache.plc4x.java.spi.codegen.io.DataWriterFactory.*;
import static org.apache.plc4x.java.spi.generation.StaticHelper.*;

import java.time.*;
import java.util.*;
import org.apache.plc4x.java.api.exceptions.*;
import org.apache.plc4x.java.api.value.*;
import org.apache.plc4x.java.spi.codegen.*;
import org.apache.plc4x.java.spi.codegen.fields.*;
import org.apache.plc4x.java.spi.codegen.io.*;
import org.apache.plc4x.java.spi.generation.*;

// Code generated by code-generation. DO NOT EDIT.

/** Variable read request reference — identifies a variable to read */
public class VariableReadRequestReference implements Message {

  // Constant values.
  public static final Short UNKNOWN1 = 0x01;

  // Properties.
  protected final byte isArray;
  protected final byte dataSizeIndex;
  protected final int block;
  protected final int baseOffset;
  protected final short offset;
  protected final Integer arrayLength;

  public VariableReadRequestReference(
      byte isArray,
      byte dataSizeIndex,
      int block,
      int baseOffset,
      short offset,
      Integer arrayLength) {
    super();
    this.isArray = isArray;
    this.dataSizeIndex = dataSizeIndex;
    this.block = block;
    this.baseOffset = baseOffset;
    this.offset = offset;
    this.arrayLength = arrayLength;
  }

  public byte getIsArray() {
    return isArray;
  }

  public byte getDataSizeIndex() {
    return dataSizeIndex;
  }

  public int getBlock() {
    return block;
  }

  public int getBaseOffset() {
    return baseOffset;
  }

  public short getOffset() {
    return offset;
  }

  public Integer getArrayLength() {
    return arrayLength;
  }

  public short getUnknown1() {
    return UNKNOWN1;
  }

  public void serialize(WriteBuffer writeBuffer) throws SerializationException {
    PositionAware positionAware = writeBuffer;
    boolean _lastItem = ThreadLocalHelper.lastItemThreadLocal.get();
    writeBuffer.pushContext("VariableReadRequestReference");

    // Simple Field (isArray)
    writeSimpleField("isArray", isArray, writeUnsignedByte(writeBuffer, 4));

    // Simple Field (dataSizeIndex)
    writeSimpleField("dataSizeIndex", dataSizeIndex, writeUnsignedByte(writeBuffer, 4));

    // Simple Field (block)
    writeSimpleField("block", block, writeUnsignedInt(writeBuffer, 16));

    // Const Field (unknown1)
    writeConstField("unknown1", UNKNOWN1, writeUnsignedShort(writeBuffer, 8));

    // Simple Field (baseOffset)
    writeSimpleField("baseOffset", baseOffset, writeUnsignedInt(writeBuffer, 16));

    // Simple Field (offset)
    writeSimpleField("offset", offset, writeUnsignedShort(writeBuffer, 8));

    // Optional Field (arrayLength) (Can be skipped, if the value is null)
    writeOptionalField("arrayLength", arrayLength, writeUnsignedInt(writeBuffer, 16));

    writeBuffer.popContext("VariableReadRequestReference");
  }

  @Override
  public int getLengthInBytes() {
    return (int) Math.ceil((float) getLengthInBits() / 8.0);
  }

  @Override
  public int getLengthInBits() {
    int lengthInBits = 0;
    VariableReadRequestReference _value = this;
    boolean _lastItem = ThreadLocalHelper.lastItemThreadLocal.get();

    // Simple field (isArray)
    lengthInBits += 4;

    // Simple field (dataSizeIndex)
    lengthInBits += 4;

    // Simple field (block)
    lengthInBits += 16;

    // Const Field (unknown1)
    lengthInBits += 8;

    // Simple field (baseOffset)
    lengthInBits += 16;

    // Simple field (offset)
    lengthInBits += 8;

    // Optional Field (arrayLength)
    if (arrayLength != null) {
      lengthInBits += 16;
    }

    return lengthInBits;
  }

  public static VariableReadRequestReference staticParse(ReadBuffer readBuffer)
      throws ParseException {
    readBuffer.pullContext("VariableReadRequestReference");
    PositionAware positionAware = readBuffer;
    boolean _lastItem = ThreadLocalHelper.lastItemThreadLocal.get();

    byte isArray = readSimpleField("isArray", readUnsignedByte(readBuffer, 4));

    byte dataSizeIndex = readSimpleField("dataSizeIndex", readUnsignedByte(readBuffer, 4));

    int block = readSimpleField("block", readUnsignedInt(readBuffer, 16));

    short unknown1 =
        readConstField(
            "unknown1", readUnsignedShort(readBuffer, 8), VariableReadRequestReference.UNKNOWN1);

    int baseOffset = readSimpleField("baseOffset", readUnsignedInt(readBuffer, 16));

    short offset = readSimpleField("offset", readUnsignedShort(readBuffer, 8));

    Integer arrayLength =
        readOptionalField("arrayLength", readUnsignedInt(readBuffer, 16), (isArray) != (0));

    readBuffer.closeContext("VariableReadRequestReference");
    // Create the instance
    VariableReadRequestReference _variableReadRequestReference;
    _variableReadRequestReference =
        new VariableReadRequestReference(
            isArray, dataSizeIndex, block, baseOffset, offset, arrayLength);
    return _variableReadRequestReference;
  }

  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (!(o instanceof VariableReadRequestReference)) {
      return false;
    }
    VariableReadRequestReference that = (VariableReadRequestReference) o;
    return (getIsArray() == that.getIsArray())
        && (getDataSizeIndex() == that.getDataSizeIndex())
        && (getBlock() == that.getBlock())
        && (getBaseOffset() == that.getBaseOffset())
        && (getOffset() == that.getOffset())
        && (getArrayLength() == that.getArrayLength())
        && true;
  }

  @Override
  public int hashCode() {
    return Objects.hash(
        getIsArray(),
        getDataSizeIndex(),
        getBlock(),
        getBaseOffset(),
        getOffset(),
        getArrayLength());
  }

  @Override
  public String toString() {
    WriteBufferBoxBased writeBufferBoxBased = new WriteBufferBoxBased(true, true);
    try {
      writeBufferBoxBased.writeSerializable(this);
    } catch (SerializationException e) {
      throw new RuntimeException(e);
    }
    return "\n" + writeBufferBoxBased.getBox().toString() + "\n";
  }
}
