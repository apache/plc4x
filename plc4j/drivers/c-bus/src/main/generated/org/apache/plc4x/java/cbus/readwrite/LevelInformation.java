// Licensed to the Apache Software Foundation (ASF) under one
// or more contributor license agreements.  See the NOTICE file
// distributed with this work for additional information
// regarding copyright ownership.  The ASF licenses this file
// to you under the Apache License, Version 2.0 (the
// "License"); you may not use this file except in compliance
// with the License.  You may obtain a copy of the License at
//
//   https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing,
// software distributed under the License is distributed on an
// "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
// KIND, either express or implied.  See the License for the
// specific language governing permissions and limitations
// under the License.
package org.apache.plc4x.java.cbus.readwrite;

import org.apache.plc4x.java.spi.buffers.api.Message;
import org.apache.plc4x.java.spi.buffers.api.ReadBuffer;
import org.apache.plc4x.java.spi.buffers.api.WithOption;
import org.apache.plc4x.java.spi.buffers.api.WriteBuffer;
import org.apache.plc4x.java.spi.buffers.api.exceptions.BufferException;
import org.apache.plc4x.java.spi.fields.data.reader.DataReaderFactory;
import org.apache.plc4x.java.spi.fields.fields.reader.FieldReaderFactory;
import org.apache.plc4x.java.spi.fields.utils.EvaluationHelper;
import org.apache.plc4x.java.spi.fields.utils.ThreadLocalHelper;

/**
 * Code generated by code-generation. DO NOT EDIT.
 */
public abstract class LevelInformation implements Message {
  protected final int raw;

  public LevelInformation(Integer raw) {
    this.raw = raw;
  }

  protected abstract void serializeLevelInformationChild(WriteBuffer writeBuffer) throws
      BufferException;

  /**
   * Property field raw
   */
  public int getRaw() {
    return raw;
  }

  /**
   * Virtual field nibble1
   */
  public byte getNibble1() {
    return (byte) ((((raw) & (0xF000))) >> (12));
  }

  /**
   * Virtual field nibble2
   */
  public byte getNibble2() {
    return (byte) ((((raw) & (0x0F00))) >> (8));
  }

  /**
   * Virtual field nibble3
   */
  public byte getNibble3() {
    return (byte) ((((raw) & (0x00F0))) >> (4));
  }

  /**
   * Virtual field nibble4
   */
  public byte getNibble4() {
    return (byte) ((((raw) & (0x000F))) >> (0));
  }

  /**
   * Virtual field isAbsent
   */
  public boolean getIsAbsent() {
    return (boolean) (((((getNibble1()) == (0x0)) && ((getNibble2()) == (0x0))) && ((getNibble3()) == (0x0))) && ((getNibble4()) == (0x0)));
  }

  /**
   * Virtual field isCorruptedByNoise
   */
  public boolean getIsCorruptedByNoise() {
    return (boolean) ((!(getIsAbsent())) && ((((((((((getNibble1()) < (0x5))) || (((getNibble1()) == (0x8)))) || (((getNibble1()) == (0xC))))) || ((((((getNibble2()) < (0x5))) || (((getNibble2()) == (0x8)))) || (((getNibble2()) == (0xC)))))) || ((((((getNibble3()) < (0x5))) || (((getNibble3()) == (0x8)))) || (((getNibble3()) == (0xC)))))) || ((((((getNibble4()) < (0x5))) || (((getNibble4()) == (0x8)))) || (((getNibble4()) == (0xC))))))));
  }

  /**
   * Virtual field isCorruptedByNoiseOrLevelsDiffer
   */
  public boolean getIsCorruptedByNoiseOrLevelsDiffer() {
    return (boolean) ((!(getIsAbsent())) && ((((((((((getNibble1()) == (0x7))) || (((getNibble1()) == (0xB)))) || (((getNibble1()) > (0xC))))) || ((((((getNibble2()) == (0x7))) || (((getNibble2()) == (0xB)))) || (((getNibble2()) > (0xC)))))) || ((((((getNibble3()) == (0x7))) || (((getNibble3()) == (0xB)))) || (((getNibble3()) > (0xC)))))) || ((((((getNibble4()) == (0x7))) || (((getNibble4()) == (0xB)))) || (((getNibble4()) > (0xC))))))));
  }

  /**
   * Virtual field isCorrupted
   */
  public boolean getIsCorrupted() {
    return (boolean) ((getIsCorruptedByNoise()) || (getIsCorruptedByNoiseOrLevelsDiffer()));
  }

  public static LevelInformation staticParse(ReadBuffer readBuffer) throws BufferException {
    readBuffer.pushContext(WithOption.WithName("LevelInformation"));
    int startPos = readBuffer.getPositionInBits();
    boolean _lastItem = ThreadLocalHelper.lastItemThreadLocal.get();
    // Peek Field: raw
    int raw = FieldReaderFactory.readPeekField(DataReaderFactory.readUnsignedInt(readBuffer, 16), WithOption.WithName("raw"));

    // Virtual Field: nibble1 (doesn't parse anything, just makes the value available)
    byte nibble1 = FieldReaderFactory.readVirtualField(byte.class, (((raw) & (0xF000))) >> (12), WithOption.WithName("nibble1"));

    // Virtual Field: nibble2 (doesn't parse anything, just makes the value available)
    byte nibble2 = FieldReaderFactory.readVirtualField(byte.class, (((raw) & (0x0F00))) >> (8), WithOption.WithName("nibble2"));

    // Virtual Field: nibble3 (doesn't parse anything, just makes the value available)
    byte nibble3 = FieldReaderFactory.readVirtualField(byte.class, (((raw) & (0x00F0))) >> (4), WithOption.WithName("nibble3"));

    // Virtual Field: nibble4 (doesn't parse anything, just makes the value available)
    byte nibble4 = FieldReaderFactory.readVirtualField(byte.class, (((raw) & (0x000F))) >> (0), WithOption.WithName("nibble4"));

    // Virtual Field: isAbsent (doesn't parse anything, just makes the value available)
    boolean isAbsent = FieldReaderFactory.readVirtualField(boolean.class, ((((nibble1) == (0x0)) && ((nibble2) == (0x0))) && ((nibble3) == (0x0))) && ((nibble4) == (0x0)), WithOption.WithName("isAbsent"));

    // Virtual Field: isCorruptedByNoise (doesn't parse anything, just makes the value available)
    boolean isCorruptedByNoise = FieldReaderFactory.readVirtualField(boolean.class, (!(isAbsent)) && ((((((((((nibble1) < (0x5))) || (((nibble1) == (0x8)))) || (((nibble1) == (0xC))))) || ((((((nibble2) < (0x5))) || (((nibble2) == (0x8)))) || (((nibble2) == (0xC)))))) || ((((((nibble3) < (0x5))) || (((nibble3) == (0x8)))) || (((nibble3) == (0xC)))))) || ((((((nibble4) < (0x5))) || (((nibble4) == (0x8)))) || (((nibble4) == (0xC))))))), WithOption.WithName("isCorruptedByNoise"));

    // Virtual Field: isCorruptedByNoiseOrLevelsDiffer (doesn't parse anything, just makes the value available)
    boolean isCorruptedByNoiseOrLevelsDiffer = FieldReaderFactory.readVirtualField(boolean.class, (!(isAbsent)) && ((((((((((nibble1) == (0x7))) || (((nibble1) == (0xB)))) || (((nibble1) > (0xC))))) || ((((((nibble2) == (0x7))) || (((nibble2) == (0xB)))) || (((nibble2) > (0xC)))))) || ((((((nibble3) == (0x7))) || (((nibble3) == (0xB)))) || (((nibble3) > (0xC)))))) || ((((((nibble4) == (0x7))) || (((nibble4) == (0xB)))) || (((nibble4) > (0xC))))))), WithOption.WithName("isCorruptedByNoiseOrLevelsDiffer"));

    // Virtual Field: isCorrupted (doesn't parse anything, just makes the value available)
    boolean isCorrupted = FieldReaderFactory.readVirtualField(boolean.class, (isCorruptedByNoise) || (isCorruptedByNoiseOrLevelsDiffer), WithOption.WithName("isCorrupted"));

    // Switch Field
    LevelInformationBuilder builder = null;
    if (EvaluationHelper.equals(isAbsent, (boolean) (true))) {
      builder = LevelInformationAbsent.staticParseLevelInformationBuilder(readBuffer);
    } else if (true && EvaluationHelper.equals(isCorrupted, (boolean) (true))) {
      builder = LevelInformationCorrupted.staticParseLevelInformationBuilder(readBuffer);
    } else if (true) {
      builder = LevelInformationNormal.staticParseLevelInformationBuilder(readBuffer);
    }
    if (builder == null) {
      throw new BufferException("Unsupported case for discriminated type parameters parameters [isAbsent, isCorrupted]");
    }

    readBuffer.popContext();
    return builder.build(raw);
  }

  @Override
  public void serialize(WriteBuffer writeBuffer) throws BufferException {
    writeBuffer.pushContext(WithOption.WithName("LevelInformation"));
    int startPos = writeBuffer.getPositionInBits();
    boolean _lastItem = ThreadLocalHelper.lastItemThreadLocal.get();
    // Peek Field: raw

    // Virtual Field: nibble1 (doesn't serialize anything, just makes the value available)
    byte nibble1 = (byte) getNibble1();

    // Virtual Field: nibble2 (doesn't serialize anything, just makes the value available)
    byte nibble2 = (byte) getNibble2();

    // Virtual Field: nibble3 (doesn't serialize anything, just makes the value available)
    byte nibble3 = (byte) getNibble3();

    // Virtual Field: nibble4 (doesn't serialize anything, just makes the value available)
    byte nibble4 = (byte) getNibble4();

    // Virtual Field: isAbsent (doesn't serialize anything, just makes the value available)
    boolean isAbsent = (boolean) getIsAbsent();

    // Virtual Field: isCorruptedByNoise (doesn't serialize anything, just makes the value available)
    boolean isCorruptedByNoise = (boolean) getIsCorruptedByNoise();

    // Virtual Field: isCorruptedByNoiseOrLevelsDiffer (doesn't serialize anything, just makes the value available)
    boolean isCorruptedByNoiseOrLevelsDiffer = (boolean) getIsCorruptedByNoiseOrLevelsDiffer();

    // Virtual Field: isCorrupted (doesn't serialize anything, just makes the value available)
    boolean isCorrupted = (boolean) getIsCorrupted();

    // Switch Field
    serializeLevelInformationChild(writeBuffer);

    writeBuffer.popContext();
  }

  @Override
  public int getLengthInBytes() {
    return (int) Math.ceil((float) getLengthInBits() / 8.0);
  }

  @Override
  public int getLengthInBits() {
    int lengthInBits = 0;
    LevelInformation _value = this;
    boolean _lastItem = ThreadLocalHelper.lastItemThreadLocal.get();
    // Peek Field: raw

    // Virtual Field: nibble1 (doesn't produce any output, just makes the value available)

    // Virtual Field: nibble2 (doesn't produce any output, just makes the value available)

    // Virtual Field: nibble3 (doesn't produce any output, just makes the value available)

    // Virtual Field: nibble4 (doesn't produce any output, just makes the value available)

    // Virtual Field: isAbsent (doesn't produce any output, just makes the value available)

    // Virtual Field: isCorruptedByNoise (doesn't produce any output, just makes the value available)

    // Virtual Field: isCorruptedByNoiseOrLevelsDiffer (doesn't produce any output, just makes the value available)

    // Virtual Field: isCorrupted (doesn't produce any output, just makes the value available)

    // Switch Field
    // (the subtype will instead add the actual length of subtype elements)

    return lengthInBits;
  }

  public interface LevelInformationBuilder {
    LevelInformation build(int raw);
  }
}
