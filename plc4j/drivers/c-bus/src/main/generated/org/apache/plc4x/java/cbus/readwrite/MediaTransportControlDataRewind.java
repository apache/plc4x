// Licensed to the Apache Software Foundation (ASF) under one
// or more contributor license agreements.  See the NOTICE file
// distributed with this work for additional information
// regarding copyright ownership.  The ASF licenses this file
// to you under the Apache License, Version 2.0 (the
// "License"); you may not use this file except in compliance
// with the License.  You may obtain a copy of the License at
//
//   https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing,
// software distributed under the License is distributed on an
// "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
// KIND, either express or implied.  See the License for the
// specific language governing permissions and limitations
// under the License.
package org.apache.plc4x.java.cbus.readwrite;

import org.apache.plc4x.java.spi.buffers.api.Message;
import org.apache.plc4x.java.spi.buffers.api.ReadBuffer;
import org.apache.plc4x.java.spi.buffers.api.WithOption;
import org.apache.plc4x.java.spi.buffers.api.WriteBuffer;
import org.apache.plc4x.java.spi.buffers.api.exceptions.BufferException;
import org.apache.plc4x.java.spi.fields.data.reader.DataReaderFactory;
import org.apache.plc4x.java.spi.fields.data.writer.DataWriterFactory;
import org.apache.plc4x.java.spi.fields.fields.reader.FieldReaderFactory;
import org.apache.plc4x.java.spi.fields.fields.writer.FieldWriterFactory;
import org.apache.plc4x.java.spi.fields.utils.ThreadLocalHelper;

/**
 * Code generated by code-generation. DO NOT EDIT.
 */
public class MediaTransportControlDataRewind extends MediaTransportControlData implements Message {
  protected final byte operation;

  public MediaTransportControlDataRewind(
      MediaTransportControlCommandTypeContainer commandTypeContainer, Byte mediaLinkGroup,
      Byte operation) {
    super(commandTypeContainer, mediaLinkGroup);
    this.operation = operation;
  }

  /**
   * Property field operation
   */
  public byte getOperation() {
    return operation;
  }

  /**
   * Virtual field isCeaseRewind
   */
  public boolean getIsCeaseRewind() {
    return (boolean) ((operation) == (0x00));
  }

  /**
   * Virtual field is2x
   */
  public boolean getIs2x() {
    return (boolean) ((operation) == (0x02));
  }

  /**
   * Virtual field is4x
   */
  public boolean getIs4x() {
    return (boolean) ((operation) == (0x04));
  }

  /**
   * Virtual field is8x
   */
  public boolean getIs8x() {
    return (boolean) ((operation) == (0x06));
  }

  /**
   * Virtual field is16x
   */
  public boolean getIs16x() {
    return (boolean) ((operation) == (0x08));
  }

  /**
   * Virtual field is32x
   */
  public boolean getIs32x() {
    return (boolean) ((operation) == (0x0A));
  }

  /**
   * Virtual field is64x
   */
  public boolean getIs64x() {
    return (boolean) ((operation) == (0x0C));
  }

  /**
   * Virtual field isReserved
   */
  public boolean getIsReserved() {
    return (boolean) (((((((!(getIsCeaseRewind())) && (!(getIs2x()))) && (!(getIs4x()))) && (!(getIs8x()))) && (!(getIs16x()))) && (!(getIs32x()))) && (!(getIs64x())));
  }

  public static MediaTransportControlDataBuilder staticParseMediaTransportControlDataBuilder(
      ReadBuffer readBuffer) throws BufferException {
    readBuffer.pushContext(WithOption.WithName("MediaTransportControlDataRewind"));
    int startPos = readBuffer.getPositionInBits();
    boolean _lastItem = ThreadLocalHelper.lastItemThreadLocal.get();
    // Simple Field: operation
    byte operation = FieldReaderFactory.readSimpleField(DataReaderFactory.readByte(readBuffer, 8), WithOption.WithName("operation"));

    // Virtual Field: isCeaseRewind (doesn't parse anything, just makes the value available)
    boolean isCeaseRewind = FieldReaderFactory.readVirtualField(boolean.class, (operation) == (0x00), WithOption.WithName("isCeaseRewind"));

    // Virtual Field: is2x (doesn't parse anything, just makes the value available)
    boolean is2x = FieldReaderFactory.readVirtualField(boolean.class, (operation) == (0x02), WithOption.WithName("is2x"));

    // Virtual Field: is4x (doesn't parse anything, just makes the value available)
    boolean is4x = FieldReaderFactory.readVirtualField(boolean.class, (operation) == (0x04), WithOption.WithName("is4x"));

    // Virtual Field: is8x (doesn't parse anything, just makes the value available)
    boolean is8x = FieldReaderFactory.readVirtualField(boolean.class, (operation) == (0x06), WithOption.WithName("is8x"));

    // Virtual Field: is16x (doesn't parse anything, just makes the value available)
    boolean is16x = FieldReaderFactory.readVirtualField(boolean.class, (operation) == (0x08), WithOption.WithName("is16x"));

    // Virtual Field: is32x (doesn't parse anything, just makes the value available)
    boolean is32x = FieldReaderFactory.readVirtualField(boolean.class, (operation) == (0x0A), WithOption.WithName("is32x"));

    // Virtual Field: is64x (doesn't parse anything, just makes the value available)
    boolean is64x = FieldReaderFactory.readVirtualField(boolean.class, (operation) == (0x0C), WithOption.WithName("is64x"));

    // Virtual Field: isReserved (doesn't parse anything, just makes the value available)
    boolean isReserved = FieldReaderFactory.readVirtualField(boolean.class, ((((((!(isCeaseRewind)) && (!(is2x))) && (!(is4x))) && (!(is8x))) && (!(is16x))) && (!(is32x))) && (!(is64x)), WithOption.WithName("isReserved"));

    readBuffer.popContext();
    return new MediaTransportControlDataBuilderImpl(operation);
  }

  protected void serializeMediaTransportControlDataChild(WriteBuffer writeBuffer) throws
      BufferException {
    writeBuffer.pushContext(WithOption.WithName("MediaTransportControlDataRewind"));
    int startPos = writeBuffer.getPositionInBits();
    boolean _lastItem = ThreadLocalHelper.lastItemThreadLocal.get();
    // Simple Field: operation
    FieldWriterFactory.writeSimpleField((byte) operation, DataWriterFactory.writeByte(writeBuffer, 8), WithOption.WithName("operation"));

    // Virtual Field: isCeaseRewind (doesn't serialize anything, just makes the value available)
    boolean isCeaseRewind = (boolean) getIsCeaseRewind();

    // Virtual Field: is2x (doesn't serialize anything, just makes the value available)
    boolean is2x = (boolean) getIs2x();

    // Virtual Field: is4x (doesn't serialize anything, just makes the value available)
    boolean is4x = (boolean) getIs4x();

    // Virtual Field: is8x (doesn't serialize anything, just makes the value available)
    boolean is8x = (boolean) getIs8x();

    // Virtual Field: is16x (doesn't serialize anything, just makes the value available)
    boolean is16x = (boolean) getIs16x();

    // Virtual Field: is32x (doesn't serialize anything, just makes the value available)
    boolean is32x = (boolean) getIs32x();

    // Virtual Field: is64x (doesn't serialize anything, just makes the value available)
    boolean is64x = (boolean) getIs64x();

    // Virtual Field: isReserved (doesn't serialize anything, just makes the value available)
    boolean isReserved = (boolean) getIsReserved();

    writeBuffer.popContext();
  }

  @Override
  public int getLengthInBytes() {
    return (int) Math.ceil((float) getLengthInBits() / 8.0);
  }

  @Override
  public int getLengthInBits() {
    int lengthInBits = super.getLengthInBits();
    MediaTransportControlDataRewind _value = this;
    boolean _lastItem = ThreadLocalHelper.lastItemThreadLocal.get();
    // Simple Field: operation
    lengthInBits += 8;

    // Virtual Field: isCeaseRewind (doesn't produce any output, just makes the value available)

    // Virtual Field: is2x (doesn't produce any output, just makes the value available)

    // Virtual Field: is4x (doesn't produce any output, just makes the value available)

    // Virtual Field: is8x (doesn't produce any output, just makes the value available)

    // Virtual Field: is16x (doesn't produce any output, just makes the value available)

    // Virtual Field: is32x (doesn't produce any output, just makes the value available)

    // Virtual Field: is64x (doesn't produce any output, just makes the value available)

    // Virtual Field: isReserved (doesn't produce any output, just makes the value available)

    return lengthInBits;
  }

  public static class MediaTransportControlDataBuilderImpl implements MediaTransportControlData.MediaTransportControlDataBuilder {
    private final byte operation;

    public MediaTransportControlDataBuilderImpl(byte operation) {
      this.operation = operation;
    }

    public MediaTransportControlData build(
        MediaTransportControlCommandTypeContainer commandTypeContainer, byte mediaLinkGroup) {
      return new MediaTransportControlDataRewind(commandTypeContainer, mediaLinkGroup, operation);
    }
  }
}
