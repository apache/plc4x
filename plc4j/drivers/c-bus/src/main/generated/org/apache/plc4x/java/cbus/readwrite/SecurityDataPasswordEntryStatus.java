// Licensed to the Apache Software Foundation (ASF) under one
// or more contributor license agreements.  See the NOTICE file
// distributed with this work for additional information
// regarding copyright ownership.  The ASF licenses this file
// to you under the Apache License, Version 2.0 (the
// "License"); you may not use this file except in compliance
// with the License.  You may obtain a copy of the License at
//
//   https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing,
// software distributed under the License is distributed on an
// "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
// KIND, either express or implied.  See the License for the
// specific language governing permissions and limitations
// under the License.
package org.apache.plc4x.java.cbus.readwrite;

import org.apache.plc4x.java.spi.buffers.api.Message;
import org.apache.plc4x.java.spi.buffers.api.ReadBuffer;
import org.apache.plc4x.java.spi.buffers.api.WithOption;
import org.apache.plc4x.java.spi.buffers.api.WriteBuffer;
import org.apache.plc4x.java.spi.buffers.api.exceptions.BufferException;
import org.apache.plc4x.java.spi.fields.data.reader.DataReaderFactory;
import org.apache.plc4x.java.spi.fields.data.writer.DataWriterFactory;
import org.apache.plc4x.java.spi.fields.fields.reader.FieldReaderFactory;
import org.apache.plc4x.java.spi.fields.fields.writer.FieldWriterFactory;
import org.apache.plc4x.java.spi.fields.utils.ThreadLocalHelper;

/**
 * Code generated by code-generation. DO NOT EDIT.
 */
public class SecurityDataPasswordEntryStatus extends SecurityData implements Message {
  protected final byte code;

  public SecurityDataPasswordEntryStatus(SecurityCommandTypeContainer commandTypeContainer,
      Byte argument, Byte code) {
    super(commandTypeContainer, argument);
    this.code = code;
  }

  /**
   * Property field code
   */
  public byte getCode() {
    return code;
  }

  /**
   * Virtual field isPasswordEntrySucceeded
   */
  public boolean getIsPasswordEntrySucceeded() {
    return (boolean) ((code) == (0x01));
  }

  /**
   * Virtual field isPasswordEntryFailed
   */
  public boolean getIsPasswordEntryFailed() {
    return (boolean) ((code) == (0x02));
  }

  /**
   * Virtual field isPasswordEntryDisabled
   */
  public boolean getIsPasswordEntryDisabled() {
    return (boolean) ((code) == (0x03));
  }

  /**
   * Virtual field isPasswordEntryEnabledAgain
   */
  public boolean getIsPasswordEntryEnabledAgain() {
    return (boolean) ((code) == (0x04));
  }

  /**
   * Virtual field isReserved
   */
  public boolean getIsReserved() {
    return (boolean) ((code) >= (0x05));
  }

  public static SecurityDataBuilder staticParseSecurityDataBuilder(ReadBuffer readBuffer) throws
      BufferException {
    readBuffer.pushContext(WithOption.WithName("SecurityDataPasswordEntryStatus"));
    int startPos = readBuffer.getPositionInBits();
    boolean _lastItem = ThreadLocalHelper.lastItemThreadLocal.get();
    // Simple Field: code
    byte code = FieldReaderFactory.readSimpleField(DataReaderFactory.readByte(readBuffer, 8), WithOption.WithName("code"));

    // Virtual Field: isPasswordEntrySucceeded (doesn't parse anything, just makes the value available)
    boolean isPasswordEntrySucceeded = FieldReaderFactory.readVirtualField(boolean.class, (code) == (0x01), WithOption.WithName("isPasswordEntrySucceeded"));

    // Virtual Field: isPasswordEntryFailed (doesn't parse anything, just makes the value available)
    boolean isPasswordEntryFailed = FieldReaderFactory.readVirtualField(boolean.class, (code) == (0x02), WithOption.WithName("isPasswordEntryFailed"));

    // Virtual Field: isPasswordEntryDisabled (doesn't parse anything, just makes the value available)
    boolean isPasswordEntryDisabled = FieldReaderFactory.readVirtualField(boolean.class, (code) == (0x03), WithOption.WithName("isPasswordEntryDisabled"));

    // Virtual Field: isPasswordEntryEnabledAgain (doesn't parse anything, just makes the value available)
    boolean isPasswordEntryEnabledAgain = FieldReaderFactory.readVirtualField(boolean.class, (code) == (0x04), WithOption.WithName("isPasswordEntryEnabledAgain"));

    // Virtual Field: isReserved (doesn't parse anything, just makes the value available)
    boolean isReserved = FieldReaderFactory.readVirtualField(boolean.class, (code) >= (0x05), WithOption.WithName("isReserved"));

    readBuffer.popContext();
    return new SecurityDataBuilderImpl(code);
  }

  protected void serializeSecurityDataChild(WriteBuffer writeBuffer) throws BufferException {
    writeBuffer.pushContext(WithOption.WithName("SecurityDataPasswordEntryStatus"));
    int startPos = writeBuffer.getPositionInBits();
    boolean _lastItem = ThreadLocalHelper.lastItemThreadLocal.get();
    // Simple Field: code
    FieldWriterFactory.writeSimpleField((byte) code, DataWriterFactory.writeByte(writeBuffer, 8), WithOption.WithName("code"));

    // Virtual Field: isPasswordEntrySucceeded (doesn't serialize anything, just makes the value available)
    boolean isPasswordEntrySucceeded = (boolean) getIsPasswordEntrySucceeded();

    // Virtual Field: isPasswordEntryFailed (doesn't serialize anything, just makes the value available)
    boolean isPasswordEntryFailed = (boolean) getIsPasswordEntryFailed();

    // Virtual Field: isPasswordEntryDisabled (doesn't serialize anything, just makes the value available)
    boolean isPasswordEntryDisabled = (boolean) getIsPasswordEntryDisabled();

    // Virtual Field: isPasswordEntryEnabledAgain (doesn't serialize anything, just makes the value available)
    boolean isPasswordEntryEnabledAgain = (boolean) getIsPasswordEntryEnabledAgain();

    // Virtual Field: isReserved (doesn't serialize anything, just makes the value available)
    boolean isReserved = (boolean) getIsReserved();

    writeBuffer.popContext();
  }

  @Override
  public int getLengthInBytes() {
    return (int) Math.ceil((float) getLengthInBits() / 8.0);
  }

  @Override
  public int getLengthInBits() {
    int lengthInBits = super.getLengthInBits();
    SecurityDataPasswordEntryStatus _value = this;
    boolean _lastItem = ThreadLocalHelper.lastItemThreadLocal.get();
    // Simple Field: code
    lengthInBits += 8;

    // Virtual Field: isPasswordEntrySucceeded (doesn't produce any output, just makes the value available)

    // Virtual Field: isPasswordEntryFailed (doesn't produce any output, just makes the value available)

    // Virtual Field: isPasswordEntryDisabled (doesn't produce any output, just makes the value available)

    // Virtual Field: isPasswordEntryEnabledAgain (doesn't produce any output, just makes the value available)

    // Virtual Field: isReserved (doesn't produce any output, just makes the value available)

    return lengthInBits;
  }

  public static class SecurityDataBuilderImpl implements SecurityData.SecurityDataBuilder {
    private final byte code;

    public SecurityDataBuilderImpl(byte code) {
      this.code = code;
    }

    public SecurityData build(SecurityCommandTypeContainer commandTypeContainer, byte argument) {
      return new SecurityDataPasswordEntryStatus(commandTypeContainer, argument, code);
    }
  }
}
