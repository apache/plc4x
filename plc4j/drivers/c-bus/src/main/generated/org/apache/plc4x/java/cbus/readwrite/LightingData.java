// Licensed to the Apache Software Foundation (ASF) under one
// or more contributor license agreements.  See the NOTICE file
// distributed with this work for additional information
// regarding copyright ownership.  The ASF licenses this file
// to you under the Apache License, Version 2.0 (the
// "License"); you may not use this file except in compliance
// with the License.  You may obtain a copy of the License at
//
//   https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing,
// software distributed under the License is distributed on an
// "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
// KIND, either express or implied.  See the License for the
// specific language governing permissions and limitations
// under the License.
package org.apache.plc4x.java.cbus.readwrite;

import org.apache.plc4x.java.cbus.readwrite.utils.StaticHelper;
import org.apache.plc4x.java.spi.buffers.api.Message;
import org.apache.plc4x.java.spi.buffers.api.ReadBuffer;
import org.apache.plc4x.java.spi.buffers.api.WithOption;
import org.apache.plc4x.java.spi.buffers.api.WriteBuffer;
import org.apache.plc4x.java.spi.buffers.api.exceptions.BufferException;
import org.apache.plc4x.java.spi.fields.data.reader.DataReaderFactory;
import org.apache.plc4x.java.spi.fields.data.writer.DataWriterFactory;
import org.apache.plc4x.java.spi.fields.exceptions.ParseAssertException;
import org.apache.plc4x.java.spi.fields.fields.reader.FieldReaderFactory;
import org.apache.plc4x.java.spi.fields.fields.writer.FieldWriterFactory;
import org.apache.plc4x.java.spi.fields.utils.EvaluationHelper;
import org.apache.plc4x.java.spi.fields.utils.ThreadLocalHelper;

/**
 * Code generated by code-generation. DO NOT EDIT.
 */
public abstract class LightingData implements Message {
  protected final LightingCommandTypeContainer commandTypeContainer;

  public LightingData(LightingCommandTypeContainer commandTypeContainer) {
    this.commandTypeContainer = commandTypeContainer;
  }

  protected abstract void serializeLightingDataChild(WriteBuffer writeBuffer) throws
      BufferException;

  /**
   * Property field commandTypeContainer
   */
  public LightingCommandTypeContainer getCommandTypeContainer() {
    return commandTypeContainer;
  }

  /**
   * Virtual field commandType
   */
  public LightingCommandType getCommandType() {
    return (LightingCommandType) (commandTypeContainer.getCommandType());
  }

  public static LightingData staticParse(ReadBuffer readBuffer) throws BufferException {
    readBuffer.pushContext(WithOption.WithName("LightingData"));
    int startPos = readBuffer.getPositionInBits();
    boolean _lastItem = ThreadLocalHelper.lastItemThreadLocal.get();
    // Validation Field
    if(!(StaticHelper.knowsLightingCommandTypeContainer(readBuffer))) {
      throw new ParseAssertException("no command type could be found");
    }

    // Simple Field (enum): commandTypeContainer
    LightingCommandTypeContainer commandTypeContainer = FieldReaderFactory.readEnumField(DataReaderFactory.readEnum(LightingCommandTypeContainer::enumForValue, DataReaderFactory.readUnsignedShort(readBuffer, 8)), WithOption.WithName("commandTypeContainer"));

    // Virtual Field: commandType (doesn't parse anything, just makes the value available)
    LightingCommandType commandType = FieldReaderFactory.readVirtualField(LightingCommandType.class, commandTypeContainer.getCommandType(), WithOption.WithName("commandType"));

    // Switch Field
    LightingDataBuilder builder = null;
    if (EvaluationHelper.equals(commandType, (LightingCommandType) (LightingCommandType.OFF))) {
      builder = LightingDataOff.staticParseLightingDataBuilder(readBuffer);
    } else if (EvaluationHelper.equals(commandType, (LightingCommandType) (LightingCommandType.ON))) {
      builder = LightingDataOn.staticParseLightingDataBuilder(readBuffer);
    } else if (EvaluationHelper.equals(commandType, (LightingCommandType) (LightingCommandType.RAMP_TO_LEVEL))) {
      builder = LightingDataRampToLevel.staticParseLightingDataBuilder(readBuffer);
    } else if (EvaluationHelper.equals(commandType, (LightingCommandType) (LightingCommandType.TERMINATE_RAMP))) {
      builder = LightingDataTerminateRamp.staticParseLightingDataBuilder(readBuffer);
    } else if (EvaluationHelper.equals(commandType, (LightingCommandType) (LightingCommandType.LABEL))) {
      builder = LightingDataLabel.staticParseLightingDataBuilder(readBuffer, commandTypeContainer);
    }
    if (builder == null) {
      throw new BufferException("Unsupported case for discriminated type parameters parameters [commandType]");
    }

    readBuffer.popContext();
    return builder.build(commandTypeContainer);
  }

  @Override
  public void serialize(WriteBuffer writeBuffer) throws BufferException {
    writeBuffer.pushContext(WithOption.WithName("LightingData"));
    int startPos = writeBuffer.getPositionInBits();
    boolean _lastItem = ThreadLocalHelper.lastItemThreadLocal.get();
    // Validation Field (Nothing needed here)

    // Simple Field (enum): commandTypeContainer
    FieldWriterFactory.writeSimpleEnumField((LightingCommandTypeContainer) commandTypeContainer, DataWriterFactory.writeEnum(LightingCommandTypeContainer::getValue, LightingCommandTypeContainer::name, DataWriterFactory.writeUnsignedShort(writeBuffer, 8)), WithOption.WithName("commandTypeContainer"));

    // Virtual Field: commandType (doesn't serialize anything, just makes the value available)
    LightingCommandType commandType = (LightingCommandType) getCommandType();

    // Switch Field
    serializeLightingDataChild(writeBuffer);

    writeBuffer.popContext();
  }

  @Override
  public int getLengthInBytes() {
    return (int) Math.ceil((float) getLengthInBits() / 8.0);
  }

  @Override
  public int getLengthInBits() {
    int lengthInBits = 0;
    LightingData _value = this;
    boolean _lastItem = ThreadLocalHelper.lastItemThreadLocal.get();
    // Validation Field (Nothing needed here)

    // Simple Field: commandTypeContainer
    lengthInBits += 8;

    // Virtual Field: commandType (doesn't produce any output, just makes the value available)

    // Switch Field
    // (the subtype will instead add the actual length of subtype elements)

    return lengthInBits;
  }

  public interface LightingDataBuilder {
    LightingData build(LightingCommandTypeContainer commandTypeContainer);
  }
}
