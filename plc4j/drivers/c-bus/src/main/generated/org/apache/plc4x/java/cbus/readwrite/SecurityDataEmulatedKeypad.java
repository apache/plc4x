// Licensed to the Apache Software Foundation (ASF) under one
// or more contributor license agreements.  See the NOTICE file
// distributed with this work for additional information
// regarding copyright ownership.  The ASF licenses this file
// to you under the Apache License, Version 2.0 (the
// "License"); you may not use this file except in compliance
// with the License.  You may obtain a copy of the License at
//
//   https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing,
// software distributed under the License is distributed on an
// "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
// KIND, either express or implied.  See the License for the
// specific language governing permissions and limitations
// under the License.
package org.apache.plc4x.java.cbus.readwrite;

import org.apache.plc4x.java.spi.buffers.api.Message;
import org.apache.plc4x.java.spi.buffers.api.ReadBuffer;
import org.apache.plc4x.java.spi.buffers.api.WithOption;
import org.apache.plc4x.java.spi.buffers.api.WriteBuffer;
import org.apache.plc4x.java.spi.buffers.api.exceptions.BufferException;
import org.apache.plc4x.java.spi.fields.data.reader.DataReaderFactory;
import org.apache.plc4x.java.spi.fields.data.writer.DataWriterFactory;
import org.apache.plc4x.java.spi.fields.fields.reader.FieldReaderFactory;
import org.apache.plc4x.java.spi.fields.fields.writer.FieldWriterFactory;
import org.apache.plc4x.java.spi.fields.utils.ThreadLocalHelper;

/**
 * Code generated by code-generation. DO NOT EDIT.
 */
public class SecurityDataEmulatedKeypad extends SecurityData implements Message {
  protected final byte key;

  public SecurityDataEmulatedKeypad(SecurityCommandTypeContainer commandTypeContainer,
      Byte argument, Byte key) {
    super(commandTypeContainer, argument);
    this.key = key;
  }

  /**
   * Property field key
   */
  public byte getKey() {
    return key;
  }

  /**
   * Virtual field isAscii
   */
  public boolean getIsAscii() {
    return (boolean) (((key) >= (0x00)) && ((key) <= (0x7F)));
  }

  /**
   * Virtual field isCustom
   */
  public boolean getIsCustom() {
    return (boolean) ((key) >= (0x80));
  }

  /**
   * Virtual field isEnter
   */
  public boolean getIsEnter() {
    return (boolean) ((key) == (0x0D));
  }

  /**
   * Virtual field isShift
   */
  public boolean getIsShift() {
    return (boolean) ((key) == (0x80));
  }

  /**
   * Virtual field isPanic
   */
  public boolean getIsPanic() {
    return (boolean) ((key) == (0x81));
  }

  /**
   * Virtual field isFire
   */
  public boolean getIsFire() {
    return (boolean) ((key) == (0x82));
  }

  /**
   * Virtual field isARM
   */
  public boolean getIsARM() {
    return (boolean) ((key) == (0x83));
  }

  /**
   * Virtual field isAway
   */
  public boolean getIsAway() {
    return (boolean) ((key) == (0x84));
  }

  /**
   * Virtual field isNight
   */
  public boolean getIsNight() {
    return (boolean) ((key) == (0x85));
  }

  /**
   * Virtual field isDay
   */
  public boolean getIsDay() {
    return (boolean) ((key) == (0x86));
  }

  /**
   * Virtual field isVacation
   */
  public boolean getIsVacation() {
    return (boolean) ((key) == (0x87));
  }

  public static SecurityDataBuilder staticParseSecurityDataBuilder(ReadBuffer readBuffer) throws
      BufferException {
    readBuffer.pushContext(WithOption.WithName("SecurityDataEmulatedKeypad"));
    int startPos = readBuffer.getPositionInBits();
    boolean _lastItem = ThreadLocalHelper.lastItemThreadLocal.get();
    // Simple Field: key
    byte key = FieldReaderFactory.readSimpleField(DataReaderFactory.readByte(readBuffer, 8), WithOption.WithName("key"));

    // Virtual Field: isAscii (doesn't parse anything, just makes the value available)
    boolean isAscii = FieldReaderFactory.readVirtualField(boolean.class, ((key) >= (0x00)) && ((key) <= (0x7F)), WithOption.WithName("isAscii"));

    // Virtual Field: isCustom (doesn't parse anything, just makes the value available)
    boolean isCustom = FieldReaderFactory.readVirtualField(boolean.class, (key) >= (0x80), WithOption.WithName("isCustom"));

    // Virtual Field: isEnter (doesn't parse anything, just makes the value available)
    boolean isEnter = FieldReaderFactory.readVirtualField(boolean.class, (key) == (0x0D), WithOption.WithName("isEnter"));

    // Virtual Field: isShift (doesn't parse anything, just makes the value available)
    boolean isShift = FieldReaderFactory.readVirtualField(boolean.class, (key) == (0x80), WithOption.WithName("isShift"));

    // Virtual Field: isPanic (doesn't parse anything, just makes the value available)
    boolean isPanic = FieldReaderFactory.readVirtualField(boolean.class, (key) == (0x81), WithOption.WithName("isPanic"));

    // Virtual Field: isFire (doesn't parse anything, just makes the value available)
    boolean isFire = FieldReaderFactory.readVirtualField(boolean.class, (key) == (0x82), WithOption.WithName("isFire"));

    // Virtual Field: isARM (doesn't parse anything, just makes the value available)
    boolean isARM = FieldReaderFactory.readVirtualField(boolean.class, (key) == (0x83), WithOption.WithName("isARM"));

    // Virtual Field: isAway (doesn't parse anything, just makes the value available)
    boolean isAway = FieldReaderFactory.readVirtualField(boolean.class, (key) == (0x84), WithOption.WithName("isAway"));

    // Virtual Field: isNight (doesn't parse anything, just makes the value available)
    boolean isNight = FieldReaderFactory.readVirtualField(boolean.class, (key) == (0x85), WithOption.WithName("isNight"));

    // Virtual Field: isDay (doesn't parse anything, just makes the value available)
    boolean isDay = FieldReaderFactory.readVirtualField(boolean.class, (key) == (0x86), WithOption.WithName("isDay"));

    // Virtual Field: isVacation (doesn't parse anything, just makes the value available)
    boolean isVacation = FieldReaderFactory.readVirtualField(boolean.class, (key) == (0x87), WithOption.WithName("isVacation"));

    readBuffer.popContext();
    return new SecurityDataBuilderImpl(key);
  }

  protected void serializeSecurityDataChild(WriteBuffer writeBuffer) throws BufferException {
    writeBuffer.pushContext(WithOption.WithName("SecurityDataEmulatedKeypad"));
    int startPos = writeBuffer.getPositionInBits();
    boolean _lastItem = ThreadLocalHelper.lastItemThreadLocal.get();
    // Simple Field: key
    FieldWriterFactory.writeSimpleField((byte) key, DataWriterFactory.writeByte(writeBuffer, 8), WithOption.WithName("key"));

    // Virtual Field: isAscii (doesn't serialize anything, just makes the value available)
    boolean isAscii = (boolean) getIsAscii();

    // Virtual Field: isCustom (doesn't serialize anything, just makes the value available)
    boolean isCustom = (boolean) getIsCustom();

    // Virtual Field: isEnter (doesn't serialize anything, just makes the value available)
    boolean isEnter = (boolean) getIsEnter();

    // Virtual Field: isShift (doesn't serialize anything, just makes the value available)
    boolean isShift = (boolean) getIsShift();

    // Virtual Field: isPanic (doesn't serialize anything, just makes the value available)
    boolean isPanic = (boolean) getIsPanic();

    // Virtual Field: isFire (doesn't serialize anything, just makes the value available)
    boolean isFire = (boolean) getIsFire();

    // Virtual Field: isARM (doesn't serialize anything, just makes the value available)
    boolean isARM = (boolean) getIsARM();

    // Virtual Field: isAway (doesn't serialize anything, just makes the value available)
    boolean isAway = (boolean) getIsAway();

    // Virtual Field: isNight (doesn't serialize anything, just makes the value available)
    boolean isNight = (boolean) getIsNight();

    // Virtual Field: isDay (doesn't serialize anything, just makes the value available)
    boolean isDay = (boolean) getIsDay();

    // Virtual Field: isVacation (doesn't serialize anything, just makes the value available)
    boolean isVacation = (boolean) getIsVacation();

    writeBuffer.popContext();
  }

  @Override
  public int getLengthInBytes() {
    return (int) Math.ceil((float) getLengthInBits() / 8.0);
  }

  @Override
  public int getLengthInBits() {
    int lengthInBits = super.getLengthInBits();
    SecurityDataEmulatedKeypad _value = this;
    boolean _lastItem = ThreadLocalHelper.lastItemThreadLocal.get();
    // Simple Field: key
    lengthInBits += 8;

    // Virtual Field: isAscii (doesn't produce any output, just makes the value available)

    // Virtual Field: isCustom (doesn't produce any output, just makes the value available)

    // Virtual Field: isEnter (doesn't produce any output, just makes the value available)

    // Virtual Field: isShift (doesn't produce any output, just makes the value available)

    // Virtual Field: isPanic (doesn't produce any output, just makes the value available)

    // Virtual Field: isFire (doesn't produce any output, just makes the value available)

    // Virtual Field: isARM (doesn't produce any output, just makes the value available)

    // Virtual Field: isAway (doesn't produce any output, just makes the value available)

    // Virtual Field: isNight (doesn't produce any output, just makes the value available)

    // Virtual Field: isDay (doesn't produce any output, just makes the value available)

    // Virtual Field: isVacation (doesn't produce any output, just makes the value available)

    return lengthInBits;
  }

  public static class SecurityDataBuilderImpl implements SecurityData.SecurityDataBuilder {
    private final byte key;

    public SecurityDataBuilderImpl(byte key) {
      this.key = key;
    }

    public SecurityData build(SecurityCommandTypeContainer commandTypeContainer, byte argument) {
      return new SecurityDataEmulatedKeypad(commandTypeContainer, argument, key);
    }
  }
}
