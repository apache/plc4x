// Licensed to the Apache Software Foundation (ASF) under one
// or more contributor license agreements.  See the NOTICE file
// distributed with this work for additional information
// regarding copyright ownership.  The ASF licenses this file
// to you under the Apache License, Version 2.0 (the
// "License"); you may not use this file except in compliance
// with the License.  You may obtain a copy of the License at
//
//   https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing,
// software distributed under the License is distributed on an
// "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
// KIND, either express or implied.  See the License for the
// specific language governing permissions and limitations
// under the License.
package org.apache.plc4x.java.cbus.readwrite;

import org.apache.plc4x.java.spi.buffers.api.Message;
import org.apache.plc4x.java.spi.buffers.api.ReadBuffer;
import org.apache.plc4x.java.spi.buffers.api.WithOption;
import org.apache.plc4x.java.spi.buffers.api.WriteBuffer;
import org.apache.plc4x.java.spi.buffers.api.exceptions.BufferException;
import org.apache.plc4x.java.spi.fields.data.reader.DataReaderFactory;
import org.apache.plc4x.java.spi.fields.data.writer.DataWriterFactory;
import org.apache.plc4x.java.spi.fields.fields.reader.FieldReaderFactory;
import org.apache.plc4x.java.spi.fields.fields.writer.FieldWriterFactory;
import org.apache.plc4x.java.spi.fields.utils.ThreadLocalHelper;

/**
 * Code generated by code-generation. DO NOT EDIT.
 */
public class PanicStatus implements Message {
  protected final short status;

  public PanicStatus(Short status) {
    this.status = status;
  }

  /**
   * Property field status
   */
  public short getStatus() {
    return status;
  }

  /**
   * Virtual field isNoPanic
   */
  public boolean getIsNoPanic() {
    return (boolean) ((status) == (0x00));
  }

  /**
   * Virtual field isReserved
   */
  public boolean getIsReserved() {
    return (boolean) (((status) >= (0x01)) && ((status) <= (0xFE)));
  }

  /**
   * Virtual field isPanicCurrentlyActive
   */
  public boolean getIsPanicCurrentlyActive() {
    return (boolean) ((status) > (0xFE));
  }

  public static PanicStatus staticParse(ReadBuffer readBuffer) throws BufferException {
    readBuffer.pushContext(WithOption.WithName("PanicStatus"));
    int startPos = readBuffer.getPositionInBits();
    boolean _lastItem = ThreadLocalHelper.lastItemThreadLocal.get();
    // Simple Field: status
    short status = FieldReaderFactory.readSimpleField(DataReaderFactory.readUnsignedShort(readBuffer, 8), WithOption.WithName("status"));

    // Virtual Field: isNoPanic (doesn't parse anything, just makes the value available)
    boolean isNoPanic = FieldReaderFactory.readVirtualField(boolean.class, (status) == (0x00), WithOption.WithName("isNoPanic"));

    // Virtual Field: isReserved (doesn't parse anything, just makes the value available)
    boolean isReserved = FieldReaderFactory.readVirtualField(boolean.class, ((status) >= (0x01)) && ((status) <= (0xFE)), WithOption.WithName("isReserved"));

    // Virtual Field: isPanicCurrentlyActive (doesn't parse anything, just makes the value available)
    boolean isPanicCurrentlyActive = FieldReaderFactory.readVirtualField(boolean.class, (status) > (0xFE), WithOption.WithName("isPanicCurrentlyActive"));

    readBuffer.popContext();
    return new PanicStatus(status);
  }

  @Override
  public void serialize(WriteBuffer writeBuffer) throws BufferException {
    writeBuffer.pushContext(WithOption.WithName("PanicStatus"));
    int startPos = writeBuffer.getPositionInBits();
    boolean _lastItem = ThreadLocalHelper.lastItemThreadLocal.get();
    // Simple Field: status
    FieldWriterFactory.writeSimpleField((short) status, DataWriterFactory.writeUnsignedShort(writeBuffer, 8), WithOption.WithName("status"));

    // Virtual Field: isNoPanic (doesn't serialize anything, just makes the value available)
    boolean isNoPanic = (boolean) getIsNoPanic();

    // Virtual Field: isReserved (doesn't serialize anything, just makes the value available)
    boolean isReserved = (boolean) getIsReserved();

    // Virtual Field: isPanicCurrentlyActive (doesn't serialize anything, just makes the value available)
    boolean isPanicCurrentlyActive = (boolean) getIsPanicCurrentlyActive();

    writeBuffer.popContext();
  }

  @Override
  public int getLengthInBytes() {
    return (int) Math.ceil((float) getLengthInBits() / 8.0);
  }

  @Override
  public int getLengthInBits() {
    int lengthInBits = 0;
    PanicStatus _value = this;
    boolean _lastItem = ThreadLocalHelper.lastItemThreadLocal.get();
    // Simple Field: status
    lengthInBits += 8;

    // Virtual Field: isNoPanic (doesn't produce any output, just makes the value available)

    // Virtual Field: isReserved (doesn't produce any output, just makes the value available)

    // Virtual Field: isPanicCurrentlyActive (doesn't produce any output, just makes the value available)

    return lengthInBits;
  }
}
