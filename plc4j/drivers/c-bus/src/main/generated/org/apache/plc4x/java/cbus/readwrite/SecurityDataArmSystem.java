// Licensed to the Apache Software Foundation (ASF) under one
// or more contributor license agreements.  See the NOTICE file
// distributed with this work for additional information
// regarding copyright ownership.  The ASF licenses this file
// to you under the Apache License, Version 2.0 (the
// "License"); you may not use this file except in compliance
// with the License.  You may obtain a copy of the License at
//
//   https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing,
// software distributed under the License is distributed on an
// "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
// KIND, either express or implied.  See the License for the
// specific language governing permissions and limitations
// under the License.
package org.apache.plc4x.java.cbus.readwrite;

import org.apache.plc4x.java.spi.buffers.api.Message;
import org.apache.plc4x.java.spi.buffers.api.ReadBuffer;
import org.apache.plc4x.java.spi.buffers.api.WithOption;
import org.apache.plc4x.java.spi.buffers.api.WriteBuffer;
import org.apache.plc4x.java.spi.buffers.api.exceptions.BufferException;
import org.apache.plc4x.java.spi.fields.data.reader.DataReaderFactory;
import org.apache.plc4x.java.spi.fields.data.writer.DataWriterFactory;
import org.apache.plc4x.java.spi.fields.fields.reader.FieldReaderFactory;
import org.apache.plc4x.java.spi.fields.fields.writer.FieldWriterFactory;
import org.apache.plc4x.java.spi.fields.utils.ThreadLocalHelper;

/**
 * Code generated by code-generation. DO NOT EDIT.
 */
public class SecurityDataArmSystem extends SecurityData implements Message {
  protected final byte armMode;

  public SecurityDataArmSystem(SecurityCommandTypeContainer commandTypeContainer, Byte argument,
      Byte armMode) {
    super(commandTypeContainer, argument);
    this.armMode = armMode;
  }

  /**
   * Property field armMode
   */
  public byte getArmMode() {
    return armMode;
  }

  /**
   * Virtual field isReserved
   */
  public boolean getIsReserved() {
    return (boolean) (((armMode) == (0x00)) || ((((armMode) >= (0x05)) && ((armMode) <= (0xFE)))));
  }

  /**
   * Virtual field isArmToAwayMode
   */
  public boolean getIsArmToAwayMode() {
    return (boolean) ((armMode) == (0x01));
  }

  /**
   * Virtual field isArmToNightMode
   */
  public boolean getIsArmToNightMode() {
    return (boolean) ((armMode) == (0x02));
  }

  /**
   * Virtual field isArmToDayMode
   */
  public boolean getIsArmToDayMode() {
    return (boolean) ((armMode) == (0x03));
  }

  /**
   * Virtual field isArmToVacationMode
   */
  public boolean getIsArmToVacationMode() {
    return (boolean) ((armMode) == (0x04));
  }

  /**
   * Virtual field isArmToHighestLevelOfProtection
   */
  public boolean getIsArmToHighestLevelOfProtection() {
    return (boolean) ((armMode) > (0xFE));
  }

  public static SecurityDataBuilder staticParseSecurityDataBuilder(ReadBuffer readBuffer) throws
      BufferException {
    readBuffer.pushContext(WithOption.WithName("SecurityDataArmSystem"));
    int startPos = readBuffer.getPositionInBits();
    boolean _lastItem = ThreadLocalHelper.lastItemThreadLocal.get();
    // Simple Field: armMode
    byte armMode = FieldReaderFactory.readSimpleField(DataReaderFactory.readByte(readBuffer, 8), WithOption.WithName("armMode"));

    // Virtual Field: isReserved (doesn't parse anything, just makes the value available)
    boolean isReserved = FieldReaderFactory.readVirtualField(boolean.class, ((armMode) == (0x00)) || ((((armMode) >= (0x05)) && ((armMode) <= (0xFE)))), WithOption.WithName("isReserved"));

    // Virtual Field: isArmToAwayMode (doesn't parse anything, just makes the value available)
    boolean isArmToAwayMode = FieldReaderFactory.readVirtualField(boolean.class, (armMode) == (0x01), WithOption.WithName("isArmToAwayMode"));

    // Virtual Field: isArmToNightMode (doesn't parse anything, just makes the value available)
    boolean isArmToNightMode = FieldReaderFactory.readVirtualField(boolean.class, (armMode) == (0x02), WithOption.WithName("isArmToNightMode"));

    // Virtual Field: isArmToDayMode (doesn't parse anything, just makes the value available)
    boolean isArmToDayMode = FieldReaderFactory.readVirtualField(boolean.class, (armMode) == (0x03), WithOption.WithName("isArmToDayMode"));

    // Virtual Field: isArmToVacationMode (doesn't parse anything, just makes the value available)
    boolean isArmToVacationMode = FieldReaderFactory.readVirtualField(boolean.class, (armMode) == (0x04), WithOption.WithName("isArmToVacationMode"));

    // Virtual Field: isArmToHighestLevelOfProtection (doesn't parse anything, just makes the value available)
    boolean isArmToHighestLevelOfProtection = FieldReaderFactory.readVirtualField(boolean.class, (armMode) > (0xFE), WithOption.WithName("isArmToHighestLevelOfProtection"));

    readBuffer.popContext();
    return new SecurityDataBuilderImpl(armMode);
  }

  protected void serializeSecurityDataChild(WriteBuffer writeBuffer) throws BufferException {
    writeBuffer.pushContext(WithOption.WithName("SecurityDataArmSystem"));
    int startPos = writeBuffer.getPositionInBits();
    boolean _lastItem = ThreadLocalHelper.lastItemThreadLocal.get();
    // Simple Field: armMode
    FieldWriterFactory.writeSimpleField((byte) armMode, DataWriterFactory.writeByte(writeBuffer, 8), WithOption.WithName("armMode"));

    // Virtual Field: isReserved (doesn't serialize anything, just makes the value available)
    boolean isReserved = (boolean) getIsReserved();

    // Virtual Field: isArmToAwayMode (doesn't serialize anything, just makes the value available)
    boolean isArmToAwayMode = (boolean) getIsArmToAwayMode();

    // Virtual Field: isArmToNightMode (doesn't serialize anything, just makes the value available)
    boolean isArmToNightMode = (boolean) getIsArmToNightMode();

    // Virtual Field: isArmToDayMode (doesn't serialize anything, just makes the value available)
    boolean isArmToDayMode = (boolean) getIsArmToDayMode();

    // Virtual Field: isArmToVacationMode (doesn't serialize anything, just makes the value available)
    boolean isArmToVacationMode = (boolean) getIsArmToVacationMode();

    // Virtual Field: isArmToHighestLevelOfProtection (doesn't serialize anything, just makes the value available)
    boolean isArmToHighestLevelOfProtection = (boolean) getIsArmToHighestLevelOfProtection();

    writeBuffer.popContext();
  }

  @Override
  public int getLengthInBytes() {
    return (int) Math.ceil((float) getLengthInBits() / 8.0);
  }

  @Override
  public int getLengthInBits() {
    int lengthInBits = super.getLengthInBits();
    SecurityDataArmSystem _value = this;
    boolean _lastItem = ThreadLocalHelper.lastItemThreadLocal.get();
    // Simple Field: armMode
    lengthInBits += 8;

    // Virtual Field: isReserved (doesn't produce any output, just makes the value available)

    // Virtual Field: isArmToAwayMode (doesn't produce any output, just makes the value available)

    // Virtual Field: isArmToNightMode (doesn't produce any output, just makes the value available)

    // Virtual Field: isArmToDayMode (doesn't produce any output, just makes the value available)

    // Virtual Field: isArmToVacationMode (doesn't produce any output, just makes the value available)

    // Virtual Field: isArmToHighestLevelOfProtection (doesn't produce any output, just makes the value available)

    return lengthInBits;
  }

  public static class SecurityDataBuilderImpl implements SecurityData.SecurityDataBuilder {
    private final byte armMode;

    public SecurityDataBuilderImpl(byte armMode) {
      this.armMode = armMode;
    }

    public SecurityData build(SecurityCommandTypeContainer commandTypeContainer, byte argument) {
      return new SecurityDataArmSystem(commandTypeContainer, argument, armMode);
    }
  }
}
