// Licensed to the Apache Software Foundation (ASF) under one
// or more contributor license agreements.  See the NOTICE file
// distributed with this work for additional information
// regarding copyright ownership.  The ASF licenses this file
// to you under the Apache License, Version 2.0 (the
// "License"); you may not use this file except in compliance
// with the License.  You may obtain a copy of the License at
//
//   https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing,
// software distributed under the License is distributed on an
// "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
// KIND, either express or implied.  See the License for the
// specific language governing permissions and limitations
// under the License.
package org.apache.plc4x.java.cbus.readwrite;

import org.apache.plc4x.java.spi.buffers.api.Message;
import org.apache.plc4x.java.spi.buffers.api.ReadBuffer;
import org.apache.plc4x.java.spi.buffers.api.WithOption;
import org.apache.plc4x.java.spi.buffers.api.WriteBuffer;
import org.apache.plc4x.java.spi.buffers.api.exceptions.BufferException;
import org.apache.plc4x.java.spi.fields.data.reader.DataReaderFactory;
import org.apache.plc4x.java.spi.fields.data.writer.DataWriterFactory;
import org.apache.plc4x.java.spi.fields.fields.reader.FieldReaderFactory;
import org.apache.plc4x.java.spi.fields.fields.writer.FieldWriterFactory;
import org.apache.plc4x.java.spi.fields.utils.ThreadLocalHelper;

/**
 * Code generated by code-generation. DO NOT EDIT.
 */
public class HVACStartTime implements Message {
  protected final int minutesSinceSunday12AM;

  public HVACStartTime(Integer minutesSinceSunday12AM) {
    this.minutesSinceSunday12AM = minutesSinceSunday12AM;
  }

  /**
   * Property field minutesSinceSunday12AM
   */
  public int getMinutesSinceSunday12AM() {
    return minutesSinceSunday12AM;
  }

  /**
   * Virtual field hoursSinceSunday12AM
   */
  public float getHoursSinceSunday12AM() {
    return (float) ((minutesSinceSunday12AM) / (60F));
  }

  /**
   * Virtual field daysSinceSunday12AM
   */
  public float getDaysSinceSunday12AM() {
    return (float) ((getHoursSinceSunday12AM()) / (24F));
  }

  /**
   * Virtual field dayOfWeek
   */
  public short getDayOfWeek() {
    return (short) ((getDaysSinceSunday12AM()) + (1));
  }

  /**
   * Virtual field hour
   */
  public short getHour() {
    return (short) ((getHoursSinceSunday12AM()) % (24));
  }

  /**
   * Virtual field minute
   */
  public short getMinute() {
    return (short) ((minutesSinceSunday12AM) % (60));
  }

  public static HVACStartTime staticParse(ReadBuffer readBuffer) throws BufferException {
    readBuffer.pushContext(WithOption.WithName("HVACStartTime"));
    int startPos = readBuffer.getPositionInBits();
    boolean _lastItem = ThreadLocalHelper.lastItemThreadLocal.get();
    // Simple Field: minutesSinceSunday12AM
    int minutesSinceSunday12AM = FieldReaderFactory.readSimpleField(DataReaderFactory.readUnsignedInt(readBuffer, 16), WithOption.WithName("minutesSinceSunday12AM"));

    // Virtual Field: hoursSinceSunday12AM (doesn't parse anything, just makes the value available)
    float hoursSinceSunday12AM = FieldReaderFactory.readVirtualField(float.class, (minutesSinceSunday12AM) / (60F), WithOption.WithName("hoursSinceSunday12AM"));

    // Virtual Field: daysSinceSunday12AM (doesn't parse anything, just makes the value available)
    float daysSinceSunday12AM = FieldReaderFactory.readVirtualField(float.class, (hoursSinceSunday12AM) / (24F), WithOption.WithName("daysSinceSunday12AM"));

    // Virtual Field: dayOfWeek (doesn't parse anything, just makes the value available)
    short dayOfWeek = FieldReaderFactory.readVirtualField(short.class, (daysSinceSunday12AM) + (1), WithOption.WithName("dayOfWeek"));

    // Virtual Field: hour (doesn't parse anything, just makes the value available)
    short hour = FieldReaderFactory.readVirtualField(short.class, (hoursSinceSunday12AM) % (24), WithOption.WithName("hour"));

    // Virtual Field: minute (doesn't parse anything, just makes the value available)
    short minute = FieldReaderFactory.readVirtualField(short.class, (minutesSinceSunday12AM) % (60), WithOption.WithName("minute"));

    readBuffer.popContext();
    return new HVACStartTime(minutesSinceSunday12AM);
  }

  @Override
  public void serialize(WriteBuffer writeBuffer) throws BufferException {
    writeBuffer.pushContext(WithOption.WithName("HVACStartTime"));
    int startPos = writeBuffer.getPositionInBits();
    boolean _lastItem = ThreadLocalHelper.lastItemThreadLocal.get();
    // Simple Field: minutesSinceSunday12AM
    FieldWriterFactory.writeSimpleField((int) minutesSinceSunday12AM, DataWriterFactory.writeUnsignedInt(writeBuffer, 16), WithOption.WithName("minutesSinceSunday12AM"));

    // Virtual Field: hoursSinceSunday12AM (doesn't serialize anything, just makes the value available)
    float hoursSinceSunday12AM = (float) getHoursSinceSunday12AM();

    // Virtual Field: daysSinceSunday12AM (doesn't serialize anything, just makes the value available)
    float daysSinceSunday12AM = (float) getDaysSinceSunday12AM();

    // Virtual Field: dayOfWeek (doesn't serialize anything, just makes the value available)
    short dayOfWeek = (short) getDayOfWeek();

    // Virtual Field: hour (doesn't serialize anything, just makes the value available)
    short hour = (short) getHour();

    // Virtual Field: minute (doesn't serialize anything, just makes the value available)
    short minute = (short) getMinute();

    writeBuffer.popContext();
  }

  @Override
  public int getLengthInBytes() {
    return (int) Math.ceil((float) getLengthInBits() / 8.0);
  }

  @Override
  public int getLengthInBits() {
    int lengthInBits = 0;
    HVACStartTime _value = this;
    boolean _lastItem = ThreadLocalHelper.lastItemThreadLocal.get();
    // Simple Field: minutesSinceSunday12AM
    lengthInBits += 16;

    // Virtual Field: hoursSinceSunday12AM (doesn't produce any output, just makes the value available)

    // Virtual Field: daysSinceSunday12AM (doesn't produce any output, just makes the value available)

    // Virtual Field: dayOfWeek (doesn't produce any output, just makes the value available)

    // Virtual Field: hour (doesn't produce any output, just makes the value available)

    // Virtual Field: minute (doesn't produce any output, just makes the value available)

    return lengthInBits;
  }
}
