// Licensed to the Apache Software Foundation (ASF) under one
// or more contributor license agreements.  See the NOTICE file
// distributed with this work for additional information
// regarding copyright ownership.  The ASF licenses this file
// to you under the Apache License, Version 2.0 (the
// "License"); you may not use this file except in compliance
// with the License.  You may obtain a copy of the License at
//
//   https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing,
// software distributed under the License is distributed on an
// "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
// KIND, either express or implied.  See the License for the
// specific language governing permissions and limitations
// under the License.
package org.apache.plc4x.java.cbus.readwrite;

import org.apache.plc4x.java.spi.buffers.api.Message;
import org.apache.plc4x.java.spi.buffers.api.ReadBuffer;
import org.apache.plc4x.java.spi.buffers.api.WithOption;
import org.apache.plc4x.java.spi.buffers.api.WriteBuffer;
import org.apache.plc4x.java.spi.buffers.api.exceptions.BufferException;
import org.apache.plc4x.java.spi.fields.data.reader.DataReaderFactory;
import org.apache.plc4x.java.spi.fields.data.writer.DataWriterFactory;
import org.apache.plc4x.java.spi.fields.fields.reader.FieldReaderFactory;
import org.apache.plc4x.java.spi.fields.fields.writer.FieldWriterFactory;
import org.apache.plc4x.java.spi.fields.utils.ThreadLocalHelper;

/**
 * Code generated by code-generation. DO NOT EDIT.
 */
public class SecurityArmCode implements Message {
  protected final short code;

  public SecurityArmCode(Short code) {
    this.code = code;
  }

  /**
   * Property field code
   */
  public short getCode() {
    return code;
  }

  /**
   * Virtual field isDisarmed
   */
  public boolean getIsDisarmed() {
    return (boolean) ((code) == (0x00));
  }

  /**
   * Virtual field isFullyArmed
   */
  public boolean getIsFullyArmed() {
    return (boolean) ((code) == (0x01));
  }

  /**
   * Virtual field isPartiallyArmed
   */
  public boolean getIsPartiallyArmed() {
    return (boolean) ((code) == (0x02));
  }

  /**
   * Virtual field isArmSubtype
   */
  public boolean getIsArmSubtype() {
    return (boolean) (((code) >= (0x03)) && ((code) <= (0x7F)));
  }

  /**
   * Virtual field isReserved
   */
  public boolean getIsReserved() {
    return (boolean) ((code) > (0x7F));
  }

  public static SecurityArmCode staticParse(ReadBuffer readBuffer) throws BufferException {
    readBuffer.pushContext(WithOption.WithName("SecurityArmCode"));
    int startPos = readBuffer.getPositionInBits();
    boolean _lastItem = ThreadLocalHelper.lastItemThreadLocal.get();
    // Simple Field: code
    short code = FieldReaderFactory.readSimpleField(DataReaderFactory.readUnsignedShort(readBuffer, 8), WithOption.WithName("code"));

    // Virtual Field: isDisarmed (doesn't parse anything, just makes the value available)
    boolean isDisarmed = FieldReaderFactory.readVirtualField(boolean.class, (code) == (0x00), WithOption.WithName("isDisarmed"));

    // Virtual Field: isFullyArmed (doesn't parse anything, just makes the value available)
    boolean isFullyArmed = FieldReaderFactory.readVirtualField(boolean.class, (code) == (0x01), WithOption.WithName("isFullyArmed"));

    // Virtual Field: isPartiallyArmed (doesn't parse anything, just makes the value available)
    boolean isPartiallyArmed = FieldReaderFactory.readVirtualField(boolean.class, (code) == (0x02), WithOption.WithName("isPartiallyArmed"));

    // Virtual Field: isArmSubtype (doesn't parse anything, just makes the value available)
    boolean isArmSubtype = FieldReaderFactory.readVirtualField(boolean.class, ((code) >= (0x03)) && ((code) <= (0x7F)), WithOption.WithName("isArmSubtype"));

    // Virtual Field: isReserved (doesn't parse anything, just makes the value available)
    boolean isReserved = FieldReaderFactory.readVirtualField(boolean.class, (code) > (0x7F), WithOption.WithName("isReserved"));

    readBuffer.popContext();
    return new SecurityArmCode(code);
  }

  @Override
  public void serialize(WriteBuffer writeBuffer) throws BufferException {
    writeBuffer.pushContext(WithOption.WithName("SecurityArmCode"));
    int startPos = writeBuffer.getPositionInBits();
    boolean _lastItem = ThreadLocalHelper.lastItemThreadLocal.get();
    // Simple Field: code
    FieldWriterFactory.writeSimpleField((short) code, DataWriterFactory.writeUnsignedShort(writeBuffer, 8), WithOption.WithName("code"));

    // Virtual Field: isDisarmed (doesn't serialize anything, just makes the value available)
    boolean isDisarmed = (boolean) getIsDisarmed();

    // Virtual Field: isFullyArmed (doesn't serialize anything, just makes the value available)
    boolean isFullyArmed = (boolean) getIsFullyArmed();

    // Virtual Field: isPartiallyArmed (doesn't serialize anything, just makes the value available)
    boolean isPartiallyArmed = (boolean) getIsPartiallyArmed();

    // Virtual Field: isArmSubtype (doesn't serialize anything, just makes the value available)
    boolean isArmSubtype = (boolean) getIsArmSubtype();

    // Virtual Field: isReserved (doesn't serialize anything, just makes the value available)
    boolean isReserved = (boolean) getIsReserved();

    writeBuffer.popContext();
  }

  @Override
  public int getLengthInBytes() {
    return (int) Math.ceil((float) getLengthInBits() / 8.0);
  }

  @Override
  public int getLengthInBits() {
    int lengthInBits = 0;
    SecurityArmCode _value = this;
    boolean _lastItem = ThreadLocalHelper.lastItemThreadLocal.get();
    // Simple Field: code
    lengthInBits += 8;

    // Virtual Field: isDisarmed (doesn't produce any output, just makes the value available)

    // Virtual Field: isFullyArmed (doesn't produce any output, just makes the value available)

    // Virtual Field: isPartiallyArmed (doesn't produce any output, just makes the value available)

    // Virtual Field: isArmSubtype (doesn't produce any output, just makes the value available)

    // Virtual Field: isReserved (doesn't produce any output, just makes the value available)

    return lengthInBits;
  }
}
