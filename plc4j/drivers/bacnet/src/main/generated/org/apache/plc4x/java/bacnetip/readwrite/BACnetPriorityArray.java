// Licensed to the Apache Software Foundation (ASF) under one
// or more contributor license agreements.  See the NOTICE file
// distributed with this work for additional information
// regarding copyright ownership.  The ASF licenses this file
// to you under the Apache License, Version 2.0 (the
// "License"); you may not use this file except in compliance
// with the License.  You may obtain a copy of the License at
//
//   https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing,
// software distributed under the License is distributed on an
// "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
// KIND, either express or implied.  See the License for the
// specific language governing permissions and limitations
// under the License.
package org.apache.plc4x.java.bacnetip.readwrite;

import java.math.BigInteger;
import java.util.List;
import org.apache.plc4x.java.spi.buffers.api.Message;
import org.apache.plc4x.java.spi.buffers.api.ReadBuffer;
import org.apache.plc4x.java.spi.buffers.api.WithOption;
import org.apache.plc4x.java.spi.buffers.api.WriteBuffer;
import org.apache.plc4x.java.spi.buffers.api.exceptions.BufferException;
import org.apache.plc4x.java.spi.fields.data.reader.DataReaderFactory;
import org.apache.plc4x.java.spi.fields.data.writer.DataWriterFactory;
import org.apache.plc4x.java.spi.fields.exceptions.ParseAssertException;
import org.apache.plc4x.java.spi.fields.fields.reader.FieldReaderFactory;
import org.apache.plc4x.java.spi.fields.fields.writer.FieldWriterFactory;
import org.apache.plc4x.java.spi.fields.utils.ThreadLocalHelper;
import org.apache.plc4x.java.spi.utils.StaticHelper;

/**
 * Code generated by code-generation. DO NOT EDIT.
 */
public class BACnetPriorityArray implements Message {
  protected final BACnetApplicationTagUnsignedInteger numberOfDataElements;

  protected final List<BACnetPriorityValue> data;

  public BACnetPriorityArray(BACnetApplicationTagUnsignedInteger numberOfDataElements,
      List<BACnetPriorityValue> data) {
    this.numberOfDataElements = numberOfDataElements;
    this.data = data;
  }

  /**
   * Property field numberOfDataElements
   */
  public BACnetApplicationTagUnsignedInteger getNumberOfDataElements() {
    return numberOfDataElements;
  }

  /**
   * Property field data
   */
  public List<BACnetPriorityValue> getData() {
    return data;
  }

  /**
   * Virtual field zero
   */
  public BigInteger getZero() {
    Number o = 0L;
    if(o instanceof BigInteger) {
      return (BigInteger) o;
    } else {
      return BigInteger.valueOf(o.longValue());
    }
  }

  /**
   * Virtual field priorityValue01
   */
  public BACnetPriorityValue getPriorityValue01() {
    return (BACnetPriorityValue) ((((StaticHelper.COUNT(data)) > (0)) ? data.get(0) : null));
  }

  /**
   * Virtual field priorityValue02
   */
  public BACnetPriorityValue getPriorityValue02() {
    return (BACnetPriorityValue) ((((StaticHelper.COUNT(data)) > (1)) ? data.get(1) : null));
  }

  /**
   * Virtual field priorityValue03
   */
  public BACnetPriorityValue getPriorityValue03() {
    return (BACnetPriorityValue) ((((StaticHelper.COUNT(data)) > (2)) ? data.get(2) : null));
  }

  /**
   * Virtual field priorityValue04
   */
  public BACnetPriorityValue getPriorityValue04() {
    return (BACnetPriorityValue) ((((StaticHelper.COUNT(data)) > (3)) ? data.get(3) : null));
  }

  /**
   * Virtual field priorityValue05
   */
  public BACnetPriorityValue getPriorityValue05() {
    return (BACnetPriorityValue) ((((StaticHelper.COUNT(data)) > (4)) ? data.get(4) : null));
  }

  /**
   * Virtual field priorityValue06
   */
  public BACnetPriorityValue getPriorityValue06() {
    return (BACnetPriorityValue) ((((StaticHelper.COUNT(data)) > (5)) ? data.get(5) : null));
  }

  /**
   * Virtual field priorityValue07
   */
  public BACnetPriorityValue getPriorityValue07() {
    return (BACnetPriorityValue) ((((StaticHelper.COUNT(data)) > (6)) ? data.get(6) : null));
  }

  /**
   * Virtual field priorityValue08
   */
  public BACnetPriorityValue getPriorityValue08() {
    return (BACnetPriorityValue) ((((StaticHelper.COUNT(data)) > (7)) ? data.get(7) : null));
  }

  /**
   * Virtual field priorityValue09
   */
  public BACnetPriorityValue getPriorityValue09() {
    return (BACnetPriorityValue) ((((StaticHelper.COUNT(data)) > (8)) ? data.get(8) : null));
  }

  /**
   * Virtual field priorityValue10
   */
  public BACnetPriorityValue getPriorityValue10() {
    return (BACnetPriorityValue) ((((StaticHelper.COUNT(data)) > (9)) ? data.get(9) : null));
  }

  /**
   * Virtual field priorityValue11
   */
  public BACnetPriorityValue getPriorityValue11() {
    return (BACnetPriorityValue) ((((StaticHelper.COUNT(data)) > (10)) ? data.get(10) : null));
  }

  /**
   * Virtual field priorityValue12
   */
  public BACnetPriorityValue getPriorityValue12() {
    return (BACnetPriorityValue) ((((StaticHelper.COUNT(data)) > (11)) ? data.get(11) : null));
  }

  /**
   * Virtual field priorityValue13
   */
  public BACnetPriorityValue getPriorityValue13() {
    return (BACnetPriorityValue) ((((StaticHelper.COUNT(data)) > (12)) ? data.get(12) : null));
  }

  /**
   * Virtual field priorityValue14
   */
  public BACnetPriorityValue getPriorityValue14() {
    return (BACnetPriorityValue) ((((StaticHelper.COUNT(data)) > (13)) ? data.get(13) : null));
  }

  /**
   * Virtual field priorityValue15
   */
  public BACnetPriorityValue getPriorityValue15() {
    return (BACnetPriorityValue) ((((StaticHelper.COUNT(data)) > (14)) ? data.get(14) : null));
  }

  /**
   * Virtual field priorityValue16
   */
  public BACnetPriorityValue getPriorityValue16() {
    return (BACnetPriorityValue) ((((StaticHelper.COUNT(data)) > (15)) ? data.get(15) : null));
  }

  /**
   * Virtual field isIndexedAccess
   */
  public boolean getIsIndexedAccess() {
    return (boolean) ((StaticHelper.COUNT(data)) == (1));
  }

  /**
   * Virtual field indexEntry
   */
  public BACnetPriorityValue getIndexEntry() {
    return (BACnetPriorityValue) (getPriorityValue01());
  }

  public static BACnetPriorityArray staticParse(ReadBuffer readBuffer,
      BACnetObjectType objectTypeArgument, short tagNumber,
      BACnetTagPayloadUnsignedInteger arrayIndexArgument) throws BufferException {
    readBuffer.pushContext(WithOption.WithName("BACnetPriorityArray"));
    int startPos = readBuffer.getPositionInBits();
    boolean _lastItem = ThreadLocalHelper.lastItemThreadLocal.get();
    // Virtual Field: zero (doesn't parse anything, just makes the value available)
    BigInteger zero = FieldReaderFactory.readVirtualField(BigInteger.class, 0L, WithOption.WithName("zero"));

    // Optional Field (conditional): numberOfDataElements
    BACnetApplicationTagUnsignedInteger numberOfDataElements = FieldReaderFactory.readOptionalField(DataReaderFactory.readComplex(() -> DataReaderFactory.castToDeclaredType(BACnetApplicationTagUnsignedInteger.class, BACnetApplicationTagUnsignedInteger.staticParse(readBuffer)), readBuffer), ((arrayIndexArgument) != (null)) && ((arrayIndexArgument.getActualValue()) == (zero)), WithOption.WithName("numberOfDataElements"));

    // Array Field: data
    List<BACnetPriorityValue> data = FieldReaderFactory.readTerminatedArrayField(DataReaderFactory.readComplex(() -> DataReaderFactory.castToDeclaredType(BACnetPriorityValue.class, BACnetPriorityValue.staticParse(readBuffer, (org.apache.plc4x.java.bacnetip.readwrite.BACnetObjectType) (objectTypeArgument))), readBuffer), () -> (boolean) org.apache.plc4x.java.bacnetip.readwrite.utils.StaticHelper.isBACnetConstructedDataClosingTag(readBuffer, false, tagNumber), WithOption.WithName("data"));

    // Virtual Field: priorityValue01 (doesn't parse anything, just makes the value available)
    BACnetPriorityValue priorityValue01 = FieldReaderFactory.readVirtualField(BACnetPriorityValue.class, (((StaticHelper.COUNT(data)) > (0)) ? data.get(0) : null), WithOption.WithName("priorityValue01"));

    // Virtual Field: priorityValue02 (doesn't parse anything, just makes the value available)
    BACnetPriorityValue priorityValue02 = FieldReaderFactory.readVirtualField(BACnetPriorityValue.class, (((StaticHelper.COUNT(data)) > (1)) ? data.get(1) : null), WithOption.WithName("priorityValue02"));

    // Virtual Field: priorityValue03 (doesn't parse anything, just makes the value available)
    BACnetPriorityValue priorityValue03 = FieldReaderFactory.readVirtualField(BACnetPriorityValue.class, (((StaticHelper.COUNT(data)) > (2)) ? data.get(2) : null), WithOption.WithName("priorityValue03"));

    // Virtual Field: priorityValue04 (doesn't parse anything, just makes the value available)
    BACnetPriorityValue priorityValue04 = FieldReaderFactory.readVirtualField(BACnetPriorityValue.class, (((StaticHelper.COUNT(data)) > (3)) ? data.get(3) : null), WithOption.WithName("priorityValue04"));

    // Virtual Field: priorityValue05 (doesn't parse anything, just makes the value available)
    BACnetPriorityValue priorityValue05 = FieldReaderFactory.readVirtualField(BACnetPriorityValue.class, (((StaticHelper.COUNT(data)) > (4)) ? data.get(4) : null), WithOption.WithName("priorityValue05"));

    // Virtual Field: priorityValue06 (doesn't parse anything, just makes the value available)
    BACnetPriorityValue priorityValue06 = FieldReaderFactory.readVirtualField(BACnetPriorityValue.class, (((StaticHelper.COUNT(data)) > (5)) ? data.get(5) : null), WithOption.WithName("priorityValue06"));

    // Virtual Field: priorityValue07 (doesn't parse anything, just makes the value available)
    BACnetPriorityValue priorityValue07 = FieldReaderFactory.readVirtualField(BACnetPriorityValue.class, (((StaticHelper.COUNT(data)) > (6)) ? data.get(6) : null), WithOption.WithName("priorityValue07"));

    // Virtual Field: priorityValue08 (doesn't parse anything, just makes the value available)
    BACnetPriorityValue priorityValue08 = FieldReaderFactory.readVirtualField(BACnetPriorityValue.class, (((StaticHelper.COUNT(data)) > (7)) ? data.get(7) : null), WithOption.WithName("priorityValue08"));

    // Virtual Field: priorityValue09 (doesn't parse anything, just makes the value available)
    BACnetPriorityValue priorityValue09 = FieldReaderFactory.readVirtualField(BACnetPriorityValue.class, (((StaticHelper.COUNT(data)) > (8)) ? data.get(8) : null), WithOption.WithName("priorityValue09"));

    // Virtual Field: priorityValue10 (doesn't parse anything, just makes the value available)
    BACnetPriorityValue priorityValue10 = FieldReaderFactory.readVirtualField(BACnetPriorityValue.class, (((StaticHelper.COUNT(data)) > (9)) ? data.get(9) : null), WithOption.WithName("priorityValue10"));

    // Virtual Field: priorityValue11 (doesn't parse anything, just makes the value available)
    BACnetPriorityValue priorityValue11 = FieldReaderFactory.readVirtualField(BACnetPriorityValue.class, (((StaticHelper.COUNT(data)) > (10)) ? data.get(10) : null), WithOption.WithName("priorityValue11"));

    // Virtual Field: priorityValue12 (doesn't parse anything, just makes the value available)
    BACnetPriorityValue priorityValue12 = FieldReaderFactory.readVirtualField(BACnetPriorityValue.class, (((StaticHelper.COUNT(data)) > (11)) ? data.get(11) : null), WithOption.WithName("priorityValue12"));

    // Virtual Field: priorityValue13 (doesn't parse anything, just makes the value available)
    BACnetPriorityValue priorityValue13 = FieldReaderFactory.readVirtualField(BACnetPriorityValue.class, (((StaticHelper.COUNT(data)) > (12)) ? data.get(12) : null), WithOption.WithName("priorityValue13"));

    // Virtual Field: priorityValue14 (doesn't parse anything, just makes the value available)
    BACnetPriorityValue priorityValue14 = FieldReaderFactory.readVirtualField(BACnetPriorityValue.class, (((StaticHelper.COUNT(data)) > (13)) ? data.get(13) : null), WithOption.WithName("priorityValue14"));

    // Virtual Field: priorityValue15 (doesn't parse anything, just makes the value available)
    BACnetPriorityValue priorityValue15 = FieldReaderFactory.readVirtualField(BACnetPriorityValue.class, (((StaticHelper.COUNT(data)) > (14)) ? data.get(14) : null), WithOption.WithName("priorityValue15"));

    // Virtual Field: priorityValue16 (doesn't parse anything, just makes the value available)
    BACnetPriorityValue priorityValue16 = FieldReaderFactory.readVirtualField(BACnetPriorityValue.class, (((StaticHelper.COUNT(data)) > (15)) ? data.get(15) : null), WithOption.WithName("priorityValue16"));

    // Validation Field
    if(!(((arrayIndexArgument) != (null)) || ((StaticHelper.COUNT(data)) == (16)))) {
      throw new ParseAssertException("Either indexed access or lenght 16 expected");
    }

    // Virtual Field: isIndexedAccess (doesn't parse anything, just makes the value available)
    boolean isIndexedAccess = FieldReaderFactory.readVirtualField(boolean.class, (StaticHelper.COUNT(data)) == (1), WithOption.WithName("isIndexedAccess"));

    // Virtual Field: indexEntry (doesn't parse anything, just makes the value available)
    BACnetPriorityValue indexEntry = FieldReaderFactory.readVirtualField(BACnetPriorityValue.class, priorityValue01, WithOption.WithName("indexEntry"));

    readBuffer.popContext();
    return new BACnetPriorityArray(numberOfDataElements, data);
  }

  @Override
  public void serialize(WriteBuffer writeBuffer) throws BufferException {
    writeBuffer.pushContext(WithOption.WithName("BACnetPriorityArray"));
    int startPos = writeBuffer.getPositionInBits();
    boolean _lastItem = ThreadLocalHelper.lastItemThreadLocal.get();
    // Virtual Field: zero (doesn't serialize anything, just makes the value available)
    BigInteger zero = (BigInteger) getZero();

    if(numberOfDataElements != null) {
      // Optional Field: numberOfDataElements
      FieldWriterFactory.writeOptionalField((BACnetApplicationTagUnsignedInteger) numberOfDataElements, DataWriterFactory.writeComplex(writeBuffer), WithOption.WithName("numberOfDataElements"));
    }

    // Array Field: data
    FieldWriterFactory.writeComplexTypeArrayField(data, writeBuffer, WithOption.WithName("data"));

    // Virtual Field: priorityValue01 (doesn't serialize anything, just makes the value available)
    BACnetPriorityValue priorityValue01 = (BACnetPriorityValue) getPriorityValue01();

    // Virtual Field: priorityValue02 (doesn't serialize anything, just makes the value available)
    BACnetPriorityValue priorityValue02 = (BACnetPriorityValue) getPriorityValue02();

    // Virtual Field: priorityValue03 (doesn't serialize anything, just makes the value available)
    BACnetPriorityValue priorityValue03 = (BACnetPriorityValue) getPriorityValue03();

    // Virtual Field: priorityValue04 (doesn't serialize anything, just makes the value available)
    BACnetPriorityValue priorityValue04 = (BACnetPriorityValue) getPriorityValue04();

    // Virtual Field: priorityValue05 (doesn't serialize anything, just makes the value available)
    BACnetPriorityValue priorityValue05 = (BACnetPriorityValue) getPriorityValue05();

    // Virtual Field: priorityValue06 (doesn't serialize anything, just makes the value available)
    BACnetPriorityValue priorityValue06 = (BACnetPriorityValue) getPriorityValue06();

    // Virtual Field: priorityValue07 (doesn't serialize anything, just makes the value available)
    BACnetPriorityValue priorityValue07 = (BACnetPriorityValue) getPriorityValue07();

    // Virtual Field: priorityValue08 (doesn't serialize anything, just makes the value available)
    BACnetPriorityValue priorityValue08 = (BACnetPriorityValue) getPriorityValue08();

    // Virtual Field: priorityValue09 (doesn't serialize anything, just makes the value available)
    BACnetPriorityValue priorityValue09 = (BACnetPriorityValue) getPriorityValue09();

    // Virtual Field: priorityValue10 (doesn't serialize anything, just makes the value available)
    BACnetPriorityValue priorityValue10 = (BACnetPriorityValue) getPriorityValue10();

    // Virtual Field: priorityValue11 (doesn't serialize anything, just makes the value available)
    BACnetPriorityValue priorityValue11 = (BACnetPriorityValue) getPriorityValue11();

    // Virtual Field: priorityValue12 (doesn't serialize anything, just makes the value available)
    BACnetPriorityValue priorityValue12 = (BACnetPriorityValue) getPriorityValue12();

    // Virtual Field: priorityValue13 (doesn't serialize anything, just makes the value available)
    BACnetPriorityValue priorityValue13 = (BACnetPriorityValue) getPriorityValue13();

    // Virtual Field: priorityValue14 (doesn't serialize anything, just makes the value available)
    BACnetPriorityValue priorityValue14 = (BACnetPriorityValue) getPriorityValue14();

    // Virtual Field: priorityValue15 (doesn't serialize anything, just makes the value available)
    BACnetPriorityValue priorityValue15 = (BACnetPriorityValue) getPriorityValue15();

    // Virtual Field: priorityValue16 (doesn't serialize anything, just makes the value available)
    BACnetPriorityValue priorityValue16 = (BACnetPriorityValue) getPriorityValue16();

    // Validation Field (Nothing needed here)

    // Virtual Field: isIndexedAccess (doesn't serialize anything, just makes the value available)
    boolean isIndexedAccess = (boolean) getIsIndexedAccess();

    // Virtual Field: indexEntry (doesn't serialize anything, just makes the value available)
    BACnetPriorityValue indexEntry = (BACnetPriorityValue) getIndexEntry();

    writeBuffer.popContext();
  }

  @Override
  public int getLengthInBytes() {
    return (int) Math.ceil((float) getLengthInBits() / 8.0);
  }

  @Override
  public int getLengthInBits() {
    int lengthInBits = 0;
    BACnetPriorityArray _value = this;
    boolean _lastItem = ThreadLocalHelper.lastItemThreadLocal.get();
    // Virtual Field: zero (doesn't produce any output, just makes the value available)

    // Optional Field: numberOfDataElements
    if(numberOfDataElements != null) {
      lengthInBits += numberOfDataElements.getLengthInBits();
    }

    // Array Field: data
    for (Message _element : data) {
      lengthInBits += _element.getLengthInBits();
    }

    // Virtual Field: priorityValue01 (doesn't produce any output, just makes the value available)

    // Virtual Field: priorityValue02 (doesn't produce any output, just makes the value available)

    // Virtual Field: priorityValue03 (doesn't produce any output, just makes the value available)

    // Virtual Field: priorityValue04 (doesn't produce any output, just makes the value available)

    // Virtual Field: priorityValue05 (doesn't produce any output, just makes the value available)

    // Virtual Field: priorityValue06 (doesn't produce any output, just makes the value available)

    // Virtual Field: priorityValue07 (doesn't produce any output, just makes the value available)

    // Virtual Field: priorityValue08 (doesn't produce any output, just makes the value available)

    // Virtual Field: priorityValue09 (doesn't produce any output, just makes the value available)

    // Virtual Field: priorityValue10 (doesn't produce any output, just makes the value available)

    // Virtual Field: priorityValue11 (doesn't produce any output, just makes the value available)

    // Virtual Field: priorityValue12 (doesn't produce any output, just makes the value available)

    // Virtual Field: priorityValue13 (doesn't produce any output, just makes the value available)

    // Virtual Field: priorityValue14 (doesn't produce any output, just makes the value available)

    // Virtual Field: priorityValue15 (doesn't produce any output, just makes the value available)

    // Virtual Field: priorityValue16 (doesn't produce any output, just makes the value available)

    // Validation Field (Nothing needed here)

    // Virtual Field: isIndexedAccess (doesn't produce any output, just makes the value available)

    // Virtual Field: indexEntry (doesn't produce any output, just makes the value available)

    return lengthInBits;
  }
}
