// Licensed to the Apache Software Foundation (ASF) under one
// or more contributor license agreements.  See the NOTICE file
// distributed with this work for additional information
// regarding copyright ownership.  The ASF licenses this file
// to you under the Apache License, Version 2.0 (the
// "License"); you may not use this file except in compliance
// with the License.  You may obtain a copy of the License at
//
//   https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing,
// software distributed under the License is distributed on an
// "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
// KIND, either express or implied.  See the License for the
// specific language governing permissions and limitations
// under the License.
package org.apache.plc4x.java.bacnetip.readwrite;

import org.apache.plc4x.java.spi.buffers.api.Message;
import org.apache.plc4x.java.spi.buffers.api.ReadBuffer;
import org.apache.plc4x.java.spi.buffers.api.WithOption;
import org.apache.plc4x.java.spi.buffers.api.WriteBuffer;
import org.apache.plc4x.java.spi.buffers.api.exceptions.BufferException;
import org.apache.plc4x.java.spi.fields.data.reader.DataReaderFactory;
import org.apache.plc4x.java.spi.fields.data.writer.DataWriterFactory;
import org.apache.plc4x.java.spi.fields.exceptions.ParseAssertException;
import org.apache.plc4x.java.spi.fields.fields.reader.FieldReaderFactory;
import org.apache.plc4x.java.spi.fields.fields.writer.FieldWriterFactory;
import org.apache.plc4x.java.spi.fields.utils.ThreadLocalHelper;
import org.apache.plc4x.java.spi.utils.StaticHelper;

/**
 * Code generated by code-generation. DO NOT EDIT.
 */
public class BACnetObjectTypesSupportedTagged implements Message {
  protected final BACnetTagHeader header;

  protected final BACnetTagPayloadBitString payload;

  public BACnetObjectTypesSupportedTagged(BACnetTagHeader header,
      BACnetTagPayloadBitString payload) {
    this.header = header;
    this.payload = payload;
  }

  /**
   * Property field header
   */
  public BACnetTagHeader getHeader() {
    return header;
  }

  /**
   * Property field payload
   */
  public BACnetTagPayloadBitString getPayload() {
    return payload;
  }

  /**
   * Virtual field timeValue
   */
  public boolean getTimeValue() {
    return (boolean) (((((StaticHelper.COUNT(payload.getData())) > (0))) ? payload.getData().get(0) : false));
  }

  /**
   * Virtual field notificationForwarder
   */
  public boolean getNotificationForwarder() {
    return (boolean) (((((StaticHelper.COUNT(payload.getData())) > (1))) ? payload.getData().get(1) : false));
  }

  /**
   * Virtual field alertEnrollment
   */
  public boolean getAlertEnrollment() {
    return (boolean) (((((StaticHelper.COUNT(payload.getData())) > (2))) ? payload.getData().get(2) : false));
  }

  /**
   * Virtual field channel
   */
  public boolean getChannel() {
    return (boolean) (((((StaticHelper.COUNT(payload.getData())) > (3))) ? payload.getData().get(3) : false));
  }

  /**
   * Virtual field lightingOutput
   */
  public boolean getLightingOutput() {
    return (boolean) (((((StaticHelper.COUNT(payload.getData())) > (4))) ? payload.getData().get(4) : false));
  }

  /**
   * Virtual field binaryLightingOutput
   */
  public boolean getBinaryLightingOutput() {
    return (boolean) (((((StaticHelper.COUNT(payload.getData())) > (5))) ? payload.getData().get(5) : false));
  }

  /**
   * Virtual field networkPort
   */
  public boolean getNetworkPort() {
    return (boolean) (((((StaticHelper.COUNT(payload.getData())) > (6))) ? payload.getData().get(6) : false));
  }

  /**
   * Virtual field elevatorGroup
   */
  public boolean getElevatorGroup() {
    return (boolean) (((((StaticHelper.COUNT(payload.getData())) > (7))) ? payload.getData().get(7) : false));
  }

  /**
   * Virtual field escalator
   */
  public boolean getEscalator() {
    return (boolean) (((((StaticHelper.COUNT(payload.getData())) > (8))) ? payload.getData().get(8) : false));
  }

  /**
   * Virtual field lift
   */
  public boolean getLift() {
    return (boolean) (((((StaticHelper.COUNT(payload.getData())) > (9))) ? payload.getData().get(9) : false));
  }

  public static BACnetObjectTypesSupportedTagged staticParse(ReadBuffer readBuffer, short tagNumber,
      TagClass tagClass) throws BufferException {
    readBuffer.pushContext(WithOption.WithName("BACnetObjectTypesSupportedTagged"));
    int startPos = readBuffer.getPositionInBits();
    boolean _lastItem = ThreadLocalHelper.lastItemThreadLocal.get();
    // Simple Field: header
    BACnetTagHeader header = FieldReaderFactory.readSimpleField(DataReaderFactory.readComplex(() -> DataReaderFactory.castToDeclaredType(BACnetTagHeader.class, BACnetTagHeader.staticParse(readBuffer)), readBuffer), WithOption.WithName("header"));

    // Validation Field
    if(!((header.getTagClass()) == (tagClass))) {
      throw new ParseAssertException("tag class doesn't match");
    }

    // Validation Field
    if(!((((header.getTagClass()) == (TagClass.APPLICATION_TAGS))) || (((header.getActualTagNumber()) == (tagNumber))))) {
      throw new ParseAssertException("tagnumber doesn't match");
    }

    // Simple Field: payload
    BACnetTagPayloadBitString payload = FieldReaderFactory.readSimpleField(DataReaderFactory.readComplex(() -> DataReaderFactory.castToDeclaredType(BACnetTagPayloadBitString.class, BACnetTagPayloadBitString.staticParse(readBuffer, (long) (header.getActualLength()))), readBuffer), WithOption.WithName("payload"));

    // Virtual Field: timeValue (doesn't parse anything, just makes the value available)
    boolean timeValue = FieldReaderFactory.readVirtualField(boolean.class, ((((StaticHelper.COUNT(payload.getData())) > (0))) ? payload.getData().get(0) : false), WithOption.WithName("timeValue"));

    // Virtual Field: notificationForwarder (doesn't parse anything, just makes the value available)
    boolean notificationForwarder = FieldReaderFactory.readVirtualField(boolean.class, ((((StaticHelper.COUNT(payload.getData())) > (1))) ? payload.getData().get(1) : false), WithOption.WithName("notificationForwarder"));

    // Virtual Field: alertEnrollment (doesn't parse anything, just makes the value available)
    boolean alertEnrollment = FieldReaderFactory.readVirtualField(boolean.class, ((((StaticHelper.COUNT(payload.getData())) > (2))) ? payload.getData().get(2) : false), WithOption.WithName("alertEnrollment"));

    // Virtual Field: channel (doesn't parse anything, just makes the value available)
    boolean channel = FieldReaderFactory.readVirtualField(boolean.class, ((((StaticHelper.COUNT(payload.getData())) > (3))) ? payload.getData().get(3) : false), WithOption.WithName("channel"));

    // Virtual Field: lightingOutput (doesn't parse anything, just makes the value available)
    boolean lightingOutput = FieldReaderFactory.readVirtualField(boolean.class, ((((StaticHelper.COUNT(payload.getData())) > (4))) ? payload.getData().get(4) : false), WithOption.WithName("lightingOutput"));

    // Virtual Field: binaryLightingOutput (doesn't parse anything, just makes the value available)
    boolean binaryLightingOutput = FieldReaderFactory.readVirtualField(boolean.class, ((((StaticHelper.COUNT(payload.getData())) > (5))) ? payload.getData().get(5) : false), WithOption.WithName("binaryLightingOutput"));

    // Virtual Field: networkPort (doesn't parse anything, just makes the value available)
    boolean networkPort = FieldReaderFactory.readVirtualField(boolean.class, ((((StaticHelper.COUNT(payload.getData())) > (6))) ? payload.getData().get(6) : false), WithOption.WithName("networkPort"));

    // Virtual Field: elevatorGroup (doesn't parse anything, just makes the value available)
    boolean elevatorGroup = FieldReaderFactory.readVirtualField(boolean.class, ((((StaticHelper.COUNT(payload.getData())) > (7))) ? payload.getData().get(7) : false), WithOption.WithName("elevatorGroup"));

    // Virtual Field: escalator (doesn't parse anything, just makes the value available)
    boolean escalator = FieldReaderFactory.readVirtualField(boolean.class, ((((StaticHelper.COUNT(payload.getData())) > (8))) ? payload.getData().get(8) : false), WithOption.WithName("escalator"));

    // Virtual Field: lift (doesn't parse anything, just makes the value available)
    boolean lift = FieldReaderFactory.readVirtualField(boolean.class, ((((StaticHelper.COUNT(payload.getData())) > (9))) ? payload.getData().get(9) : false), WithOption.WithName("lift"));

    readBuffer.popContext();
    return new BACnetObjectTypesSupportedTagged(header, payload);
  }

  @Override
  public void serialize(WriteBuffer writeBuffer) throws BufferException {
    writeBuffer.pushContext(WithOption.WithName("BACnetObjectTypesSupportedTagged"));
    int startPos = writeBuffer.getPositionInBits();
    boolean _lastItem = ThreadLocalHelper.lastItemThreadLocal.get();
    // Simple Field: header
    FieldWriterFactory.writeSimpleField((BACnetTagHeader) header, DataWriterFactory.writeComplex(writeBuffer), WithOption.WithName("header"));

    // Validation Field (Nothing needed here)

    // Validation Field (Nothing needed here)

    // Simple Field: payload
    FieldWriterFactory.writeSimpleField((BACnetTagPayloadBitString) payload, DataWriterFactory.writeComplex(writeBuffer), WithOption.WithName("payload"));

    // Virtual Field: timeValue (doesn't serialize anything, just makes the value available)
    boolean timeValue = (boolean) getTimeValue();

    // Virtual Field: notificationForwarder (doesn't serialize anything, just makes the value available)
    boolean notificationForwarder = (boolean) getNotificationForwarder();

    // Virtual Field: alertEnrollment (doesn't serialize anything, just makes the value available)
    boolean alertEnrollment = (boolean) getAlertEnrollment();

    // Virtual Field: channel (doesn't serialize anything, just makes the value available)
    boolean channel = (boolean) getChannel();

    // Virtual Field: lightingOutput (doesn't serialize anything, just makes the value available)
    boolean lightingOutput = (boolean) getLightingOutput();

    // Virtual Field: binaryLightingOutput (doesn't serialize anything, just makes the value available)
    boolean binaryLightingOutput = (boolean) getBinaryLightingOutput();

    // Virtual Field: networkPort (doesn't serialize anything, just makes the value available)
    boolean networkPort = (boolean) getNetworkPort();

    // Virtual Field: elevatorGroup (doesn't serialize anything, just makes the value available)
    boolean elevatorGroup = (boolean) getElevatorGroup();

    // Virtual Field: escalator (doesn't serialize anything, just makes the value available)
    boolean escalator = (boolean) getEscalator();

    // Virtual Field: lift (doesn't serialize anything, just makes the value available)
    boolean lift = (boolean) getLift();

    writeBuffer.popContext();
  }

  @Override
  public int getLengthInBytes() {
    return (int) Math.ceil((float) getLengthInBits() / 8.0);
  }

  @Override
  public int getLengthInBits() {
    int lengthInBits = 0;
    BACnetObjectTypesSupportedTagged _value = this;
    boolean _lastItem = ThreadLocalHelper.lastItemThreadLocal.get();
    // Simple Field: header
    lengthInBits += header.getLengthInBits();

    // Validation Field (Nothing needed here)

    // Validation Field (Nothing needed here)

    // Simple Field: payload
    lengthInBits += payload.getLengthInBits();

    // Virtual Field: timeValue (doesn't produce any output, just makes the value available)

    // Virtual Field: notificationForwarder (doesn't produce any output, just makes the value available)

    // Virtual Field: alertEnrollment (doesn't produce any output, just makes the value available)

    // Virtual Field: channel (doesn't produce any output, just makes the value available)

    // Virtual Field: lightingOutput (doesn't produce any output, just makes the value available)

    // Virtual Field: binaryLightingOutput (doesn't produce any output, just makes the value available)

    // Virtual Field: networkPort (doesn't produce any output, just makes the value available)

    // Virtual Field: elevatorGroup (doesn't produce any output, just makes the value available)

    // Virtual Field: escalator (doesn't produce any output, just makes the value available)

    // Virtual Field: lift (doesn't produce any output, just makes the value available)

    return lengthInBits;
  }
}
