// Licensed to the Apache Software Foundation (ASF) under one
// or more contributor license agreements.  See the NOTICE file
// distributed with this work for additional information
// regarding copyright ownership.  The ASF licenses this file
// to you under the Apache License, Version 2.0 (the
// "License"); you may not use this file except in compliance
// with the License.  You may obtain a copy of the License at
//
//   https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing,
// software distributed under the License is distributed on an
// "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
// KIND, either express or implied.  See the License for the
// specific language governing permissions and limitations
// under the License.
package org.apache.plc4x.java.bacnetip.readwrite;

import org.apache.plc4x.java.spi.buffers.api.Message;
import org.apache.plc4x.java.spi.buffers.api.ReadBuffer;
import org.apache.plc4x.java.spi.buffers.api.WithOption;
import org.apache.plc4x.java.spi.buffers.api.WriteBuffer;
import org.apache.plc4x.java.spi.buffers.api.exceptions.BufferException;
import org.apache.plc4x.java.spi.fields.data.reader.DataReaderFactory;
import org.apache.plc4x.java.spi.fields.data.writer.DataWriterFactory;
import org.apache.plc4x.java.spi.fields.exceptions.ParseAssertException;
import org.apache.plc4x.java.spi.fields.fields.reader.FieldReaderFactory;
import org.apache.plc4x.java.spi.fields.fields.writer.FieldWriterFactory;
import org.apache.plc4x.java.spi.fields.utils.ThreadLocalHelper;
import org.apache.plc4x.java.spi.utils.StaticHelper;

/**
 * Code generated by code-generation. DO NOT EDIT.
 */
public class BACnetServicesSupportedTagged implements Message {
  protected final BACnetTagHeader header;

  protected final BACnetTagPayloadBitString payload;

  public BACnetServicesSupportedTagged(BACnetTagHeader header, BACnetTagPayloadBitString payload) {
    this.header = header;
    this.payload = payload;
  }

  /**
   * Property field header
   */
  public BACnetTagHeader getHeader() {
    return header;
  }

  /**
   * Property field payload
   */
  public BACnetTagPayloadBitString getPayload() {
    return payload;
  }

  /**
   * Virtual field writeGroup
   */
  public boolean getWriteGroup() {
    return (boolean) (((((StaticHelper.COUNT(payload.getData())) > (0))) ? payload.getData().get(0) : false));
  }

  /**
   * Virtual field subscribeCovPropertyMultiple
   */
  public boolean getSubscribeCovPropertyMultiple() {
    return (boolean) (((((StaticHelper.COUNT(payload.getData())) > (1))) ? payload.getData().get(1) : false));
  }

  /**
   * Virtual field confirmedCovNotificationMultiple
   */
  public boolean getConfirmedCovNotificationMultiple() {
    return (boolean) (((((StaticHelper.COUNT(payload.getData())) > (2))) ? payload.getData().get(2) : false));
  }

  /**
   * Virtual field unconfirmedCovNotificationMultiple
   */
  public boolean getUnconfirmedCovNotificationMultiple() {
    return (boolean) (((((StaticHelper.COUNT(payload.getData())) > (3))) ? payload.getData().get(3) : false));
  }

  /**
   * Virtual field whoIs
   */
  public boolean getWhoIs() {
    return (boolean) (((((StaticHelper.COUNT(payload.getData())) > (4))) ? payload.getData().get(4) : false));
  }

  /**
   * Virtual field readRange
   */
  public boolean getReadRange() {
    return (boolean) (((((StaticHelper.COUNT(payload.getData())) > (5))) ? payload.getData().get(5) : false));
  }

  /**
   * Virtual field utcTimeSynchronization
   */
  public boolean getUtcTimeSynchronization() {
    return (boolean) (((((StaticHelper.COUNT(payload.getData())) > (6))) ? payload.getData().get(6) : false));
  }

  /**
   * Virtual field lifeSafetyOperation
   */
  public boolean getLifeSafetyOperation() {
    return (boolean) (((((StaticHelper.COUNT(payload.getData())) > (7))) ? payload.getData().get(7) : false));
  }

  /**
   * Virtual field subscribeCovProperty
   */
  public boolean getSubscribeCovProperty() {
    return (boolean) (((((StaticHelper.COUNT(payload.getData())) > (8))) ? payload.getData().get(8) : false));
  }

  /**
   * Virtual field getEventInformation
   */
  public boolean getGetEventInformation() {
    return (boolean) (((((StaticHelper.COUNT(payload.getData())) > (9))) ? payload.getData().get(9) : false));
  }

  public static BACnetServicesSupportedTagged staticParse(ReadBuffer readBuffer, short tagNumber,
      TagClass tagClass) throws BufferException {
    readBuffer.pushContext(WithOption.WithName("BACnetServicesSupportedTagged"));
    int startPos = readBuffer.getPositionInBits();
    boolean _lastItem = ThreadLocalHelper.lastItemThreadLocal.get();
    // Simple Field: header
    BACnetTagHeader header = FieldReaderFactory.readSimpleField(DataReaderFactory.readComplex(() -> (BACnetTagHeader) BACnetTagHeader.staticParse(readBuffer), readBuffer), WithOption.WithName("header"));

    // Validation Field
    if(!((header.getTagClass()) == (tagClass))) {
      throw new ParseAssertException("tag class doesn't match");
    }

    // Validation Field
    if(!((((header.getTagClass()) == (TagClass.APPLICATION_TAGS))) || (((header.getActualTagNumber()) == (tagNumber))))) {
      throw new ParseAssertException("tagnumber doesn't match");
    }

    // Simple Field: payload
    BACnetTagPayloadBitString payload = FieldReaderFactory.readSimpleField(DataReaderFactory.readComplex(() -> (BACnetTagPayloadBitString) BACnetTagPayloadBitString.staticParse(readBuffer, (long) (header.getActualLength())), readBuffer), WithOption.WithName("payload"));

    // Virtual Field: writeGroup (doesn't parse anything, just makes the value available)
    boolean writeGroup = FieldReaderFactory.readVirtualField(boolean.class, ((((StaticHelper.COUNT(payload.getData())) > (0))) ? payload.getData().get(0) : false), WithOption.WithName("writeGroup"));

    // Virtual Field: subscribeCovPropertyMultiple (doesn't parse anything, just makes the value available)
    boolean subscribeCovPropertyMultiple = FieldReaderFactory.readVirtualField(boolean.class, ((((StaticHelper.COUNT(payload.getData())) > (1))) ? payload.getData().get(1) : false), WithOption.WithName("subscribeCovPropertyMultiple"));

    // Virtual Field: confirmedCovNotificationMultiple (doesn't parse anything, just makes the value available)
    boolean confirmedCovNotificationMultiple = FieldReaderFactory.readVirtualField(boolean.class, ((((StaticHelper.COUNT(payload.getData())) > (2))) ? payload.getData().get(2) : false), WithOption.WithName("confirmedCovNotificationMultiple"));

    // Virtual Field: unconfirmedCovNotificationMultiple (doesn't parse anything, just makes the value available)
    boolean unconfirmedCovNotificationMultiple = FieldReaderFactory.readVirtualField(boolean.class, ((((StaticHelper.COUNT(payload.getData())) > (3))) ? payload.getData().get(3) : false), WithOption.WithName("unconfirmedCovNotificationMultiple"));

    // Virtual Field: whoIs (doesn't parse anything, just makes the value available)
    boolean whoIs = FieldReaderFactory.readVirtualField(boolean.class, ((((StaticHelper.COUNT(payload.getData())) > (4))) ? payload.getData().get(4) : false), WithOption.WithName("whoIs"));

    // Virtual Field: readRange (doesn't parse anything, just makes the value available)
    boolean readRange = FieldReaderFactory.readVirtualField(boolean.class, ((((StaticHelper.COUNT(payload.getData())) > (5))) ? payload.getData().get(5) : false), WithOption.WithName("readRange"));

    // Virtual Field: utcTimeSynchronization (doesn't parse anything, just makes the value available)
    boolean utcTimeSynchronization = FieldReaderFactory.readVirtualField(boolean.class, ((((StaticHelper.COUNT(payload.getData())) > (6))) ? payload.getData().get(6) : false), WithOption.WithName("utcTimeSynchronization"));

    // Virtual Field: lifeSafetyOperation (doesn't parse anything, just makes the value available)
    boolean lifeSafetyOperation = FieldReaderFactory.readVirtualField(boolean.class, ((((StaticHelper.COUNT(payload.getData())) > (7))) ? payload.getData().get(7) : false), WithOption.WithName("lifeSafetyOperation"));

    // Virtual Field: subscribeCovProperty (doesn't parse anything, just makes the value available)
    boolean subscribeCovProperty = FieldReaderFactory.readVirtualField(boolean.class, ((((StaticHelper.COUNT(payload.getData())) > (8))) ? payload.getData().get(8) : false), WithOption.WithName("subscribeCovProperty"));

    // Virtual Field: getEventInformation (doesn't parse anything, just makes the value available)
    boolean getEventInformation = FieldReaderFactory.readVirtualField(boolean.class, ((((StaticHelper.COUNT(payload.getData())) > (9))) ? payload.getData().get(9) : false), WithOption.WithName("getEventInformation"));

    readBuffer.popContext();
    return new BACnetServicesSupportedTagged(header, payload);
  }

  @Override
  public void serialize(WriteBuffer writeBuffer) throws BufferException {
    writeBuffer.pushContext(WithOption.WithName("BACnetServicesSupportedTagged"));
    int startPos = writeBuffer.getPositionInBits();
    boolean _lastItem = ThreadLocalHelper.lastItemThreadLocal.get();
    // Simple Field: header
    FieldWriterFactory.writeSimpleField((BACnetTagHeader) header, DataWriterFactory.writeComplex(writeBuffer), WithOption.WithName("header"));

    // Validation Field (Nothing needed here)

    // Validation Field (Nothing needed here)

    // Simple Field: payload
    FieldWriterFactory.writeSimpleField((BACnetTagPayloadBitString) payload, DataWriterFactory.writeComplex(writeBuffer), WithOption.WithName("payload"));

    // Virtual Field: writeGroup (doesn't serialize anything, just makes the value available)
    boolean writeGroup = (boolean) getWriteGroup();

    // Virtual Field: subscribeCovPropertyMultiple (doesn't serialize anything, just makes the value available)
    boolean subscribeCovPropertyMultiple = (boolean) getSubscribeCovPropertyMultiple();

    // Virtual Field: confirmedCovNotificationMultiple (doesn't serialize anything, just makes the value available)
    boolean confirmedCovNotificationMultiple = (boolean) getConfirmedCovNotificationMultiple();

    // Virtual Field: unconfirmedCovNotificationMultiple (doesn't serialize anything, just makes the value available)
    boolean unconfirmedCovNotificationMultiple = (boolean) getUnconfirmedCovNotificationMultiple();

    // Virtual Field: whoIs (doesn't serialize anything, just makes the value available)
    boolean whoIs = (boolean) getWhoIs();

    // Virtual Field: readRange (doesn't serialize anything, just makes the value available)
    boolean readRange = (boolean) getReadRange();

    // Virtual Field: utcTimeSynchronization (doesn't serialize anything, just makes the value available)
    boolean utcTimeSynchronization = (boolean) getUtcTimeSynchronization();

    // Virtual Field: lifeSafetyOperation (doesn't serialize anything, just makes the value available)
    boolean lifeSafetyOperation = (boolean) getLifeSafetyOperation();

    // Virtual Field: subscribeCovProperty (doesn't serialize anything, just makes the value available)
    boolean subscribeCovProperty = (boolean) getSubscribeCovProperty();

    // Virtual Field: getEventInformation (doesn't serialize anything, just makes the value available)
    boolean getEventInformation = (boolean) getGetEventInformation();

    writeBuffer.popContext();
  }

  @Override
  public int getLengthInBytes() {
    return (int) Math.ceil((float) getLengthInBits() / 8.0);
  }

  @Override
  public int getLengthInBits() {
    int lengthInBits = 0;
    BACnetServicesSupportedTagged _value = this;
    boolean _lastItem = ThreadLocalHelper.lastItemThreadLocal.get();
    // Simple Field: header
    lengthInBits += header.getLengthInBits();

    // Validation Field (Nothing needed here)

    // Validation Field (Nothing needed here)

    // Simple Field: payload
    lengthInBits += payload.getLengthInBits();

    // Virtual Field: writeGroup (doesn't produce any output, just makes the value available)

    // Virtual Field: subscribeCovPropertyMultiple (doesn't produce any output, just makes the value available)

    // Virtual Field: confirmedCovNotificationMultiple (doesn't produce any output, just makes the value available)

    // Virtual Field: unconfirmedCovNotificationMultiple (doesn't produce any output, just makes the value available)

    // Virtual Field: whoIs (doesn't produce any output, just makes the value available)

    // Virtual Field: readRange (doesn't produce any output, just makes the value available)

    // Virtual Field: utcTimeSynchronization (doesn't produce any output, just makes the value available)

    // Virtual Field: lifeSafetyOperation (doesn't produce any output, just makes the value available)

    // Virtual Field: subscribeCovProperty (doesn't produce any output, just makes the value available)

    // Virtual Field: getEventInformation (doesn't produce any output, just makes the value available)

    return lengthInBits;
  }
}
