// Licensed to the Apache Software Foundation (ASF) under one
// or more contributor license agreements.  See the NOTICE file
// distributed with this work for additional information
// regarding copyright ownership.  The ASF licenses this file
// to you under the Apache License, Version 2.0 (the
// "License"); you may not use this file except in compliance
// with the License.  You may obtain a copy of the License at
//
//   https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing,
// software distributed under the License is distributed on an
// "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
// KIND, either express or implied.  See the License for the
// specific language governing permissions and limitations
// under the License.
package org.apache.plc4x.java.bacnetip.readwrite;

import org.apache.plc4x.java.spi.buffers.api.Message;
import org.apache.plc4x.java.spi.buffers.api.ReadBuffer;
import org.apache.plc4x.java.spi.buffers.api.WithOption;
import org.apache.plc4x.java.spi.buffers.api.WriteBuffer;
import org.apache.plc4x.java.spi.buffers.api.exceptions.BufferException;
import org.apache.plc4x.java.spi.fields.data.reader.DataReaderFactory;
import org.apache.plc4x.java.spi.fields.data.writer.DataWriterFactory;
import org.apache.plc4x.java.spi.fields.exceptions.ParseAssertException;
import org.apache.plc4x.java.spi.fields.fields.reader.FieldReaderFactory;
import org.apache.plc4x.java.spi.fields.fields.writer.FieldWriterFactory;
import org.apache.plc4x.java.spi.fields.utils.ThreadLocalHelper;
import org.apache.plc4x.java.spi.utils.StaticHelper;

/**
 * Code generated by code-generation. DO NOT EDIT.
 */
public class BACnetDaysOfWeekTagged implements Message {
  protected final BACnetTagHeader header;

  protected final BACnetTagPayloadBitString payload;

  public BACnetDaysOfWeekTagged(BACnetTagHeader header, BACnetTagPayloadBitString payload) {
    this.header = header;
    this.payload = payload;
  }

  /**
   * Property field header
   */
  public BACnetTagHeader getHeader() {
    return header;
  }

  /**
   * Property field payload
   */
  public BACnetTagPayloadBitString getPayload() {
    return payload;
  }

  /**
   * Virtual field monday
   */
  public boolean getMonday() {
    return (boolean) (((((StaticHelper.COUNT(payload.getData())) > (0))) ? payload.getData().get(0) : false));
  }

  /**
   * Virtual field tuesday
   */
  public boolean getTuesday() {
    return (boolean) (((((StaticHelper.COUNT(payload.getData())) > (1))) ? payload.getData().get(1) : false));
  }

  /**
   * Virtual field wednesday
   */
  public boolean getWednesday() {
    return (boolean) (((((StaticHelper.COUNT(payload.getData())) > (2))) ? payload.getData().get(2) : false));
  }

  /**
   * Virtual field thursday
   */
  public boolean getThursday() {
    return (boolean) (((((StaticHelper.COUNT(payload.getData())) > (3))) ? payload.getData().get(3) : false));
  }

  /**
   * Virtual field friday
   */
  public boolean getFriday() {
    return (boolean) (((((StaticHelper.COUNT(payload.getData())) > (4))) ? payload.getData().get(4) : false));
  }

  /**
   * Virtual field saturday
   */
  public boolean getSaturday() {
    return (boolean) (((((StaticHelper.COUNT(payload.getData())) > (5))) ? payload.getData().get(5) : false));
  }

  /**
   * Virtual field sunday
   */
  public boolean getSunday() {
    return (boolean) (((((StaticHelper.COUNT(payload.getData())) > (6))) ? payload.getData().get(6) : false));
  }

  public static BACnetDaysOfWeekTagged staticParse(ReadBuffer readBuffer, short tagNumber,
      TagClass tagClass) throws BufferException {
    readBuffer.pushContext(WithOption.WithName("BACnetDaysOfWeekTagged"));
    int startPos = readBuffer.getPositionInBits();
    boolean _lastItem = ThreadLocalHelper.lastItemThreadLocal.get();
    // Simple Field: header
    BACnetTagHeader header = FieldReaderFactory.readSimpleField(DataReaderFactory.readComplex(() -> DataReaderFactory.castToDeclaredType(BACnetTagHeader.class, BACnetTagHeader.staticParse(readBuffer)), readBuffer), WithOption.WithName("header"));

    // Validation Field
    if(!((header.getTagClass()) == (tagClass))) {
      throw new ParseAssertException("tag class doesn't match");
    }

    // Validation Field
    if(!((((header.getTagClass()) == (TagClass.APPLICATION_TAGS))) || (((header.getActualTagNumber()) == (tagNumber))))) {
      throw new ParseAssertException("tagnumber doesn't match");
    }

    // Simple Field: payload
    BACnetTagPayloadBitString payload = FieldReaderFactory.readSimpleField(DataReaderFactory.readComplex(() -> DataReaderFactory.castToDeclaredType(BACnetTagPayloadBitString.class, BACnetTagPayloadBitString.staticParse(readBuffer, (long) (header.getActualLength()))), readBuffer), WithOption.WithName("payload"));

    // Virtual Field: monday (doesn't parse anything, just makes the value available)
    boolean monday = FieldReaderFactory.readVirtualField(boolean.class, ((((StaticHelper.COUNT(payload.getData())) > (0))) ? payload.getData().get(0) : false), WithOption.WithName("monday"));

    // Virtual Field: tuesday (doesn't parse anything, just makes the value available)
    boolean tuesday = FieldReaderFactory.readVirtualField(boolean.class, ((((StaticHelper.COUNT(payload.getData())) > (1))) ? payload.getData().get(1) : false), WithOption.WithName("tuesday"));

    // Virtual Field: wednesday (doesn't parse anything, just makes the value available)
    boolean wednesday = FieldReaderFactory.readVirtualField(boolean.class, ((((StaticHelper.COUNT(payload.getData())) > (2))) ? payload.getData().get(2) : false), WithOption.WithName("wednesday"));

    // Virtual Field: thursday (doesn't parse anything, just makes the value available)
    boolean thursday = FieldReaderFactory.readVirtualField(boolean.class, ((((StaticHelper.COUNT(payload.getData())) > (3))) ? payload.getData().get(3) : false), WithOption.WithName("thursday"));

    // Virtual Field: friday (doesn't parse anything, just makes the value available)
    boolean friday = FieldReaderFactory.readVirtualField(boolean.class, ((((StaticHelper.COUNT(payload.getData())) > (4))) ? payload.getData().get(4) : false), WithOption.WithName("friday"));

    // Virtual Field: saturday (doesn't parse anything, just makes the value available)
    boolean saturday = FieldReaderFactory.readVirtualField(boolean.class, ((((StaticHelper.COUNT(payload.getData())) > (5))) ? payload.getData().get(5) : false), WithOption.WithName("saturday"));

    // Virtual Field: sunday (doesn't parse anything, just makes the value available)
    boolean sunday = FieldReaderFactory.readVirtualField(boolean.class, ((((StaticHelper.COUNT(payload.getData())) > (6))) ? payload.getData().get(6) : false), WithOption.WithName("sunday"));

    readBuffer.popContext();
    return new BACnetDaysOfWeekTagged(header, payload);
  }

  @Override
  public void serialize(WriteBuffer writeBuffer) throws BufferException {
    writeBuffer.pushContext(WithOption.WithName("BACnetDaysOfWeekTagged"));
    int startPos = writeBuffer.getPositionInBits();
    boolean _lastItem = ThreadLocalHelper.lastItemThreadLocal.get();
    // Simple Field: header
    FieldWriterFactory.writeSimpleField((BACnetTagHeader) header, DataWriterFactory.writeComplex(writeBuffer), WithOption.WithName("header"));

    // Validation Field (Nothing needed here)

    // Validation Field (Nothing needed here)

    // Simple Field: payload
    FieldWriterFactory.writeSimpleField((BACnetTagPayloadBitString) payload, DataWriterFactory.writeComplex(writeBuffer), WithOption.WithName("payload"));

    // Virtual Field: monday (doesn't serialize anything, just makes the value available)
    boolean monday = (boolean) getMonday();

    // Virtual Field: tuesday (doesn't serialize anything, just makes the value available)
    boolean tuesday = (boolean) getTuesday();

    // Virtual Field: wednesday (doesn't serialize anything, just makes the value available)
    boolean wednesday = (boolean) getWednesday();

    // Virtual Field: thursday (doesn't serialize anything, just makes the value available)
    boolean thursday = (boolean) getThursday();

    // Virtual Field: friday (doesn't serialize anything, just makes the value available)
    boolean friday = (boolean) getFriday();

    // Virtual Field: saturday (doesn't serialize anything, just makes the value available)
    boolean saturday = (boolean) getSaturday();

    // Virtual Field: sunday (doesn't serialize anything, just makes the value available)
    boolean sunday = (boolean) getSunday();

    writeBuffer.popContext();
  }

  @Override
  public int getLengthInBytes() {
    return (int) Math.ceil((float) getLengthInBits() / 8.0);
  }

  @Override
  public int getLengthInBits() {
    int lengthInBits = 0;
    BACnetDaysOfWeekTagged _value = this;
    boolean _lastItem = ThreadLocalHelper.lastItemThreadLocal.get();
    // Simple Field: header
    lengthInBits += header.getLengthInBits();

    // Validation Field (Nothing needed here)

    // Validation Field (Nothing needed here)

    // Simple Field: payload
    lengthInBits += payload.getLengthInBits();

    // Virtual Field: monday (doesn't produce any output, just makes the value available)

    // Virtual Field: tuesday (doesn't produce any output, just makes the value available)

    // Virtual Field: wednesday (doesn't produce any output, just makes the value available)

    // Virtual Field: thursday (doesn't produce any output, just makes the value available)

    // Virtual Field: friday (doesn't produce any output, just makes the value available)

    // Virtual Field: saturday (doesn't produce any output, just makes the value available)

    // Virtual Field: sunday (doesn't produce any output, just makes the value available)

    return lengthInBits;
  }
}
