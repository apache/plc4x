// Licensed to the Apache Software Foundation (ASF) under one
// or more contributor license agreements.  See the NOTICE file
// distributed with this work for additional information
// regarding copyright ownership.  The ASF licenses this file
// to you under the Apache License, Version 2.0 (the
// "License"); you may not use this file except in compliance
// with the License.  You may obtain a copy of the License at
//
//   https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing,
// software distributed under the License is distributed on an
// "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
// KIND, either express or implied.  See the License for the
// specific language governing permissions and limitations
// under the License.
package org.apache.plc4x.java.bacnetip.readwrite;

import org.apache.plc4x.java.spi.buffers.api.Message;
import org.apache.plc4x.java.spi.buffers.api.ReadBuffer;
import org.apache.plc4x.java.spi.buffers.api.WithOption;
import org.apache.plc4x.java.spi.buffers.api.WriteBuffer;
import org.apache.plc4x.java.spi.buffers.api.exceptions.BufferException;
import org.apache.plc4x.java.spi.fields.data.reader.DataReaderFactory;
import org.apache.plc4x.java.spi.fields.data.writer.DataWriterFactory;
import org.apache.plc4x.java.spi.fields.exceptions.ParseAssertException;
import org.apache.plc4x.java.spi.fields.fields.reader.FieldReaderFactory;
import org.apache.plc4x.java.spi.fields.fields.writer.FieldWriterFactory;
import org.apache.plc4x.java.spi.fields.utils.ThreadLocalHelper;
import org.apache.plc4x.java.spi.utils.StaticHelper;

/**
 * Code generated by code-generation. DO NOT EDIT.
 */
public class BACnetResultFlagsTagged implements Message {
  protected final BACnetTagHeader header;

  protected final BACnetTagPayloadBitString payload;

  public BACnetResultFlagsTagged(BACnetTagHeader header, BACnetTagPayloadBitString payload) {
    this.header = header;
    this.payload = payload;
  }

  /**
   * Property field header
   */
  public BACnetTagHeader getHeader() {
    return header;
  }

  /**
   * Property field payload
   */
  public BACnetTagPayloadBitString getPayload() {
    return payload;
  }

  /**
   * Virtual field firstItem
   */
  public boolean getFirstItem() {
    return (boolean) (((((StaticHelper.COUNT(payload.getData())) > (0))) ? payload.getData().get(0) : false));
  }

  /**
   * Virtual field lastItem
   */
  public boolean getLastItem() {
    return (boolean) (((((StaticHelper.COUNT(payload.getData())) > (1))) ? payload.getData().get(1) : false));
  }

  /**
   * Virtual field moreItems
   */
  public boolean getMoreItems() {
    return (boolean) (((((StaticHelper.COUNT(payload.getData())) > (2))) ? payload.getData().get(2) : false));
  }

  public static BACnetResultFlagsTagged staticParse(ReadBuffer readBuffer, short tagNumber,
      TagClass tagClass) throws BufferException {
    readBuffer.pushContext(WithOption.WithName("BACnetResultFlagsTagged"));
    int startPos = readBuffer.getPositionInBits();
    boolean _lastItem = ThreadLocalHelper.lastItemThreadLocal.get();
    // Simple Field: header
    BACnetTagHeader header = FieldReaderFactory.readSimpleField(DataReaderFactory.readComplex(() -> (BACnetTagHeader) BACnetTagHeader.staticParse(readBuffer), readBuffer), WithOption.WithName("header"));

    // Validation Field
    if(!((header.getTagClass()) == (tagClass))) {
      throw new ParseAssertException("tag class doesn't match");
    }

    // Validation Field
    if(!((((header.getTagClass()) == (TagClass.APPLICATION_TAGS))) || (((header.getActualTagNumber()) == (tagNumber))))) {
      throw new ParseAssertException("tagnumber doesn't match");
    }

    // Simple Field: payload
    BACnetTagPayloadBitString payload = FieldReaderFactory.readSimpleField(DataReaderFactory.readComplex(() -> (BACnetTagPayloadBitString) BACnetTagPayloadBitString.staticParse(readBuffer, (long) (header.getActualLength())), readBuffer), WithOption.WithName("payload"));

    // Virtual Field: firstItem (doesn't parse anything, just makes the value available)
    boolean firstItem = FieldReaderFactory.readVirtualField(boolean.class, ((((StaticHelper.COUNT(payload.getData())) > (0))) ? payload.getData().get(0) : false), WithOption.WithName("firstItem"));

    // Virtual Field: lastItem (doesn't parse anything, just makes the value available)
    boolean lastItem = FieldReaderFactory.readVirtualField(boolean.class, ((((StaticHelper.COUNT(payload.getData())) > (1))) ? payload.getData().get(1) : false), WithOption.WithName("lastItem"));

    // Virtual Field: moreItems (doesn't parse anything, just makes the value available)
    boolean moreItems = FieldReaderFactory.readVirtualField(boolean.class, ((((StaticHelper.COUNT(payload.getData())) > (2))) ? payload.getData().get(2) : false), WithOption.WithName("moreItems"));

    readBuffer.popContext();
    return new BACnetResultFlagsTagged(header, payload);
  }

  @Override
  public void serialize(WriteBuffer writeBuffer) throws BufferException {
    writeBuffer.pushContext(WithOption.WithName("BACnetResultFlagsTagged"));
    int startPos = writeBuffer.getPositionInBits();
    boolean _lastItem = ThreadLocalHelper.lastItemThreadLocal.get();
    // Simple Field: header
    FieldWriterFactory.writeSimpleField((BACnetTagHeader) header, DataWriterFactory.writeComplex(writeBuffer), WithOption.WithName("header"));

    // Validation Field (Nothing needed here)

    // Validation Field (Nothing needed here)

    // Simple Field: payload
    FieldWriterFactory.writeSimpleField((BACnetTagPayloadBitString) payload, DataWriterFactory.writeComplex(writeBuffer), WithOption.WithName("payload"));

    // Virtual Field: firstItem (doesn't serialize anything, just makes the value available)
    boolean firstItem = (boolean) getFirstItem();

    // Virtual Field: lastItem (doesn't serialize anything, just makes the value available)
    boolean lastItem = (boolean) getLastItem();

    // Virtual Field: moreItems (doesn't serialize anything, just makes the value available)
    boolean moreItems = (boolean) getMoreItems();

    writeBuffer.popContext();
  }

  @Override
  public int getLengthInBytes() {
    return (int) Math.ceil((float) getLengthInBits() / 8.0);
  }

  @Override
  public int getLengthInBits() {
    int lengthInBits = 0;
    BACnetResultFlagsTagged _value = this;
    boolean _lastItem = ThreadLocalHelper.lastItemThreadLocal.get();
    // Simple Field: header
    lengthInBits += header.getLengthInBits();

    // Validation Field (Nothing needed here)

    // Validation Field (Nothing needed here)

    // Simple Field: payload
    lengthInBits += payload.getLengthInBits();

    // Virtual Field: firstItem (doesn't produce any output, just makes the value available)

    // Virtual Field: lastItem (doesn't produce any output, just makes the value available)

    // Virtual Field: moreItems (doesn't produce any output, just makes the value available)

    return lengthInBits;
  }
}
