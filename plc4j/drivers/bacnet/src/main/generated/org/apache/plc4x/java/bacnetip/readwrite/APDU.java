// Licensed to the Apache Software Foundation (ASF) under one
// or more contributor license agreements.  See the NOTICE file
// distributed with this work for additional information
// regarding copyright ownership.  The ASF licenses this file
// to you under the Apache License, Version 2.0 (the
// "License"); you may not use this file except in compliance
// with the License.  You may obtain a copy of the License at
//
//   https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing,
// software distributed under the License is distributed on an
// "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
// KIND, either express or implied.  See the License for the
// specific language governing permissions and limitations
// under the License.
package org.apache.plc4x.java.bacnetip.readwrite;

import org.apache.plc4x.java.spi.buffers.api.Message;
import org.apache.plc4x.java.spi.buffers.api.ReadBuffer;
import org.apache.plc4x.java.spi.buffers.api.WithOption;
import org.apache.plc4x.java.spi.buffers.api.WriteBuffer;
import org.apache.plc4x.java.spi.buffers.api.exceptions.BufferException;
import org.apache.plc4x.java.spi.fields.data.reader.DataReaderFactory;
import org.apache.plc4x.java.spi.fields.data.writer.DataWriterFactory;
import org.apache.plc4x.java.spi.fields.fields.reader.FieldReaderFactory;
import org.apache.plc4x.java.spi.fields.fields.writer.FieldWriterFactory;
import org.apache.plc4x.java.spi.fields.utils.EvaluationHelper;
import org.apache.plc4x.java.spi.fields.utils.ThreadLocalHelper;

/**
 * Code generated by code-generation. DO NOT EDIT.
 */
public abstract class APDU implements Message {
  public APDU() {
  }

  /**
   * Discriminator field apduType
   */
  public abstract ApduType getApduType();

  protected abstract void serializeAPDUChild(WriteBuffer writeBuffer) throws BufferException;

  public static APDU staticParse(ReadBuffer readBuffer, int apduLength) throws BufferException {
    readBuffer.pushContext(WithOption.WithName("APDU"));
    int startPos = readBuffer.getPositionInBits();
    boolean _lastItem = ThreadLocalHelper.lastItemThreadLocal.get();
    // Enum Discriminator Field: apduType
    ApduType apduType = FieldReaderFactory.readDiscriminatorEnumField(DataReaderFactory.readEnum(ApduType::enumForValue, DataReaderFactory.readUnsignedByte(readBuffer, 4)), WithOption.WithName("apduType"));

    // Switch Field
    APDUBuilder builder = null;
    if (EvaluationHelper.equals(apduType, (ApduType) (ApduType.CONFIRMED_REQUEST_PDU))) {
      builder = APDUConfirmedRequest.staticParseAPDUBuilder(readBuffer, apduLength);
    } else if (EvaluationHelper.equals(apduType, (ApduType) (ApduType.UNCONFIRMED_REQUEST_PDU))) {
      builder = APDUUnconfirmedRequest.staticParseAPDUBuilder(readBuffer, apduLength);
    } else if (EvaluationHelper.equals(apduType, (ApduType) (ApduType.SIMPLE_ACK_PDU))) {
      builder = APDUSimpleAck.staticParseAPDUBuilder(readBuffer, apduLength);
    } else if (EvaluationHelper.equals(apduType, (ApduType) (ApduType.COMPLEX_ACK_PDU))) {
      builder = APDUComplexAck.staticParseAPDUBuilder(readBuffer, apduLength);
    } else if (EvaluationHelper.equals(apduType, (ApduType) (ApduType.SEGMENT_ACK_PDU))) {
      builder = APDUSegmentAck.staticParseAPDUBuilder(readBuffer, apduLength);
    } else if (EvaluationHelper.equals(apduType, (ApduType) (ApduType.ERROR_PDU))) {
      builder = APDUError.staticParseAPDUBuilder(readBuffer, apduLength);
    } else if (EvaluationHelper.equals(apduType, (ApduType) (ApduType.REJECT_PDU))) {
      builder = APDUReject.staticParseAPDUBuilder(readBuffer, apduLength);
    } else if (EvaluationHelper.equals(apduType, (ApduType) (ApduType.ABORT_PDU))) {
      builder = APDUAbort.staticParseAPDUBuilder(readBuffer, apduLength);
    } else if (true) {
      builder = APDUUnknown.staticParseAPDUBuilder(readBuffer, apduLength);
    }
    if (builder == null) {
      throw new BufferException("Unsupported case for discriminated type parameters parameters [apduType]");
    }

    readBuffer.popContext();
    return builder.build();
  }

  @Override
  public void serialize(WriteBuffer writeBuffer) throws BufferException {
    writeBuffer.pushContext(WithOption.WithName("APDU"));
    int startPos = writeBuffer.getPositionInBits();
    boolean _lastItem = ThreadLocalHelper.lastItemThreadLocal.get();
    ApduType apduType = (ApduType) getApduType();
    // Enum Discriminator Field: apduType
    FieldWriterFactory.writeDiscriminatorEnumField((ApduType) apduType, DataWriterFactory.writeEnum(ApduType::getValue, ApduType::name, DataWriterFactory.writeUnsignedByte(writeBuffer, 4)), WithOption.WithName("apduType"));

    // Switch Field
    serializeAPDUChild(writeBuffer);

    writeBuffer.popContext();
  }

  @Override
  public int getLengthInBytes() {
    return (int) Math.ceil((float) getLengthInBits() / 8.0);
  }

  @Override
  public int getLengthInBits() {
    int lengthInBits = 0;
    APDU _value = this;
    boolean _lastItem = ThreadLocalHelper.lastItemThreadLocal.get();
    // Discriminator Field: apduType
    lengthInBits += 4;

    // Switch Field
    // (the subtype will instead add the actual length of subtype elements)

    return lengthInBits;
  }

  public interface APDUBuilder {
    APDU build();
  }
}
