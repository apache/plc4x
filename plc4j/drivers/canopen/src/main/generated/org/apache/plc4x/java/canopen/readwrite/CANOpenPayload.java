// Licensed to the Apache Software Foundation (ASF) under one
// or more contributor license agreements.  See the NOTICE file
// distributed with this work for additional information
// regarding copyright ownership.  The ASF licenses this file
// to you under the Apache License, Version 2.0 (the
// "License"); you may not use this file except in compliance
// with the License.  You may obtain a copy of the License at
//
//   https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing,
// software distributed under the License is distributed on an
// "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
// KIND, either express or implied.  See the License for the
// specific language governing permissions and limitations
// under the License.
package org.apache.plc4x.java.canopen.readwrite;

import org.apache.plc4x.java.spi.buffers.api.Message;
import org.apache.plc4x.java.spi.buffers.api.ReadBuffer;
import org.apache.plc4x.java.spi.buffers.api.WithOption;
import org.apache.plc4x.java.spi.buffers.api.WriteBuffer;
import org.apache.plc4x.java.spi.buffers.api.exceptions.BufferException;
import org.apache.plc4x.java.spi.buffers.bytebased.WithByteBasedOption;
import org.apache.plc4x.java.spi.fields.utils.EvaluationHelper;
import org.apache.plc4x.java.spi.fields.utils.ThreadLocalHelper;

/**
 * Code generated by code-generation. DO NOT EDIT.
 */
public abstract class CANOpenPayload implements Message {
  public CANOpenPayload() {
  }

  /**
   * Discriminator field service
   */
  public abstract CANOpenService getService();

  protected abstract void serializeCANOpenPayloadChild(WriteBuffer writeBuffer) throws
      BufferException;

  public static CANOpenPayload staticParse(ReadBuffer readBuffer, CANOpenService service) throws
      BufferException {
    readBuffer.pushContext(WithOption.WithName("CANOpenPayload"), WithOption.WithFloatEncoding("IEEE754"), WithOption.WithSignedIntegerEncoding("twos-complement"), WithOption.WithUnsignedIntegerEncoding("unsigned-binary"), WithByteBasedOption.WithByteOrder("LITTLE_ENDIAN"), WithOption.WithStringEncoding("UTF8"));
    int startPos = readBuffer.getPositionInBits();
    boolean _lastItem = ThreadLocalHelper.lastItemThreadLocal.get();
    // Switch Field
    CANOpenPayloadBuilder builder = null;
    if (EvaluationHelper.equals(service, (CANOpenService) (CANOpenService.NMT))) {
      builder = CANOpenNetworkPayload.staticParseCANOpenPayloadBuilder(readBuffer, service);
    } else if (EvaluationHelper.equals(service, (CANOpenService) (CANOpenService.TIME))) {
      builder = CANOpenTimeSynchronization.staticParseCANOpenPayloadBuilder(readBuffer, service);
    } else if (EvaluationHelper.equals(service, (CANOpenService) (CANOpenService.RECEIVE_PDO_1))) {
      builder = CANOpenPDOPayload.staticParseCANOpenPayloadBuilder(readBuffer, service);
    } else if (EvaluationHelper.equals(service, (CANOpenService) (CANOpenService.TRANSMIT_PDO_1))) {
      builder = CANOpenPDOPayload.staticParseCANOpenPayloadBuilder(readBuffer, service);
    } else if (EvaluationHelper.equals(service, (CANOpenService) (CANOpenService.RECEIVE_PDO_2))) {
      builder = CANOpenPDOPayload.staticParseCANOpenPayloadBuilder(readBuffer, service);
    } else if (EvaluationHelper.equals(service, (CANOpenService) (CANOpenService.TRANSMIT_PDO_2))) {
      builder = CANOpenPDOPayload.staticParseCANOpenPayloadBuilder(readBuffer, service);
    } else if (EvaluationHelper.equals(service, (CANOpenService) (CANOpenService.RECEIVE_PDO_3))) {
      builder = CANOpenPDOPayload.staticParseCANOpenPayloadBuilder(readBuffer, service);
    } else if (EvaluationHelper.equals(service, (CANOpenService) (CANOpenService.TRANSMIT_PDO_3))) {
      builder = CANOpenPDOPayload.staticParseCANOpenPayloadBuilder(readBuffer, service);
    } else if (EvaluationHelper.equals(service, (CANOpenService) (CANOpenService.RECEIVE_PDO_4))) {
      builder = CANOpenPDOPayload.staticParseCANOpenPayloadBuilder(readBuffer, service);
    } else if (EvaluationHelper.equals(service, (CANOpenService) (CANOpenService.TRANSMIT_PDO_4))) {
      builder = CANOpenPDOPayload.staticParseCANOpenPayloadBuilder(readBuffer, service);
    } else if (EvaluationHelper.equals(service, (CANOpenService) (CANOpenService.RECEIVE_SDO))) {
      builder = CANOpenSDORequest.staticParseCANOpenPayloadBuilder(readBuffer, service);
    } else if (EvaluationHelper.equals(service, (CANOpenService) (CANOpenService.TRANSMIT_SDO))) {
      builder = CANOpenSDOResponse.staticParseCANOpenPayloadBuilder(readBuffer, service);
    } else if (EvaluationHelper.equals(service, (CANOpenService) (CANOpenService.HEARTBEAT))) {
      builder = CANOpenHeartbeatPayload.staticParseCANOpenPayloadBuilder(readBuffer, service);
    }
    if (builder == null) {
      throw new BufferException("Unsupported case for discriminated type parameters parameters [service]");
    }

    readBuffer.popContext();
    return builder.build();
  }

  @Override
  public void serialize(WriteBuffer writeBuffer) throws BufferException {
    writeBuffer.pushContext(WithOption.WithName("CANOpenPayload"), WithOption.WithFloatEncoding("IEEE754"), WithOption.WithSignedIntegerEncoding("twos-complement"), WithOption.WithUnsignedIntegerEncoding("unsigned-binary"), WithByteBasedOption.WithByteOrder("LITTLE_ENDIAN"), WithOption.WithStringEncoding("UTF8"));
    int startPos = writeBuffer.getPositionInBits();
    boolean _lastItem = ThreadLocalHelper.lastItemThreadLocal.get();
    // Switch Field
    serializeCANOpenPayloadChild(writeBuffer);

    writeBuffer.popContext();
  }

  @Override
  public int getLengthInBytes() {
    return (int) Math.ceil((float) getLengthInBits() / 8.0);
  }

  @Override
  public int getLengthInBits() {
    int lengthInBits = 0;
    CANOpenPayload _value = this;
    boolean _lastItem = ThreadLocalHelper.lastItemThreadLocal.get();
    // Switch Field
    // (the subtype will instead add the actual length of subtype elements)

    return lengthInBits;
  }

  public interface CANOpenPayloadBuilder {
    CANOpenPayload build();
  }
}
